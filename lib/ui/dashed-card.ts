// ОДИН РИСУНОК ПУНКТИРНОЙ КАРТОЧКИ НА ВСЕХ, КТО ЕЁ ПОКАЗЫВАЕТ (шаг 71, 2026-08-31).
//
// 🔒 ЗАЧЕМ ОТДЕЛЬНЫЙ ФАЙЛ РАДИ ОДНОЙ СТРОКИ КЛАССОВ. Потребителей стало двое, и
// они разной природы: КЛИЕНТСКИЙ островок заявки (кнопка — открывает окно) и
// СЕРВЕРНЫЙ вид каталога `invite` (ссылка — ведёт по адресу). Общего кода у них
// быть не может, общий у них ВИД. Скопировать классы значило бы завести вторую
// пунктирную карточку, которая разойдётся с первой на первой же правке темы —
// ✗ ровно этим оплачен шаг 70, где я нарисовал свою кнопку вместо существующей.
//
// 🔒 РАЗНИЦА ДВУХ ПОТРЕБИТЕЛЕЙ НАЗВАНА, А НЕ СТЁРТА. Островок — действие, у него
// `<button>` и окно; блок — содержимое, у него `<a>` и адрес. Одинаковыми они
// обязаны быть только на вид: человек, увидев такую рамку, уже знает, что это
// приглашение что-то добавить, а не готовая вещь.
export const DASHED_CARD =
  "flex w-full items-center justify-center gap-2 rounded-lg border border-dashed border-border p-6 " +
  "text-[length:var(--fs-body)] font-medium text-muted-foreground transition-colors " +
  "hover:border-primary/50 hover:bg-muted/40 hover:text-foreground"
