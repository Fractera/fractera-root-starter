{
  "id": "root",
  "name": "Fractera Root",
  "summary": "The site of the node: what a visitor sees at the root of the domain. A replaceable element — any repository that keeps this passport's contract can take its place.",
  "kind": "microservice",
  "entry": "global",
  "port": {
    "desired": 24683,
    "note": "A wish, not a decision. The node assigns the actual port from the block 24680-24699. Ask the node, never remember."
  },
  "env": {
    "file": ".env.local",
    "example": ".env.example",
    "note": "Next reads .env.local by its own convention."
  },
  "health": {
    "path": "/api/health",
    "expect": 200,
    "note": "Runs the site's own code and needs neither a session nor a key. Answers pid and startedAt so the node can tell this process from another one on the same name."
  },
  "runtime": {
    "stack": "next",
    "node": ">=20",
    "build": "npm run build",
    "start": "node .next/standalone/<traced path>/server.js",
    "startNote": "The standalone server reads PORT and HOSTNAME from the environment. The installer finds server.js under .next/standalone and copies .next/static next to it.",
    "nativeModules": []
  },
  "contract": [
    "Takes its port from the environment; never remembers a number.",
    "Answers GET /api/health with 200 without a session.",
    "Signs people in only through the node's auth element; never carries its own sign-in.",
    "Reaches stored data only through the node's data element; never opens a database file.",
    "Public pages stay static: no force-dynamic, no cookies() or headers() in a layout or a page."
  ],
  "provides": ["site"],
  "languages": {
    "count": 1,
    "note": "The starter page is English only; the node's public pages arrive in a later version with their own languages."
  },
  "license": "MIT"
}
