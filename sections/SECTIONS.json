{
  "_": "Generated by npm run build:blocks-map. Hand edits are lost.",
  "generatedFrom": [
    "sections/index.ts",
    "sections/taxonomy.json",
    "sections/blocks/<kind>.md",
    "app/[lang]/(publicLayer)/**/_data/en.ts"
  ],
  "types": [
    {
      "id": "hero",
      "order": 1,
      "title": {
        "en": "Hero",
        "ru": "Герой"
      },
      "purpose": {
        "en": "First impression, clear positioning and the main offer above the fold.",
        "ru": "Первое впечатление, чёткое позиционирование и главный оффер над «линией сгиба»."
      },
      "variants": {
        "en": "Classic (heading + text + button + visual), hero with a signup form, minimal with video background, hero slider.",
        "ru": "Классический (заголовок + текст + кнопка + визуал), с формой записи, минималистичный с видео-фоном, слайдер."
      }
    },
    {
      "id": "benefits",
      "order": 2,
      "title": {
        "en": "Benefits and value",
        "ru": "Преимущества и ценности"
      },
      "purpose": {
        "en": "The key benefit for the user, told as claims with icons.",
        "ru": "Раскрытие ключевой пользы продукта через тезисы и иконки."
      },
      "variants": {
        "en": "A 2x2 or 3x2 grid of cards, blocks with large icons, alternating rows (Z-pattern).",
        "ru": "Сетка карточек 2×2 или 3×2, блоки с крупными иконками, чередующиеся строки (Z-паттерн)."
      }
    },
    {
      "id": "how-it-works",
      "order": 3,
      "title": {
        "en": "How it works",
        "ru": "Как это работает"
      },
      "purpose": {
        "en": "A step-by-step explanation of the purchase, the interaction or the service.",
        "ru": "Пошаговое объяснение процесса покупки, взаимодействия или работы сервиса."
      },
      "variants": {
        "en": "Horizontal step cards (1-2-3), a vertical timeline, an animated demo with stage switching.",
        "ru": "Горизонтальные карточки шагов (1-2-3), вертикальный таймлайн, демонстрация с переключением этапов."
      }
    },
    {
      "id": "product-demo",
      "order": 4,
      "title": {
        "en": "Product in action",
        "ru": "Продукт в действии"
      },
      "purpose": {
        "en": "Showing the interface, the physical product or a key feature.",
        "ru": "Наглядный показ интерфейса, физического товара или ключевой функции."
      },
      "variants": {
        "en": "Interactive tabs, a video walkthrough, cards with hover effects, a clickable mock-up.",
        "ru": "Интерактивные вкладки, видео-демонстрация, карточки с эффектами наведения, кликабельный макет."
      }
    },
    {
      "id": "use-cases",
      "order": 5,
      "title": {
        "en": "Use cases",
        "ru": "Сценарии использования"
      },
      "purpose": {
        "en": "Segmenting the audience: how the product solves the tasks of a role or an industry.",
        "ru": "Сегментация аудитории — как продукт решает задачи конкретных ролей или отраслей."
      },
      "variants": {
        "en": "Tabs by role (\"for marketers\", \"for developers\"), problem-and-solution cards.",
        "ru": "Вкладки по ролям («для маркетологов», «для разработчиков»), карточки проблем и решений."
      }
    },
    {
      "id": "comparison",
      "order": 6,
      "title": {
        "en": "Comparison",
        "ru": "Сравнение"
      },
      "purpose": {
        "en": "Setting the product apart from competitors or from the old way of solving the problem.",
        "ru": "Отстройка от конкурентов или от старых способов решения задачи."
      },
      "variants": {
        "en": "A tick-and-cross table, \"before / after\" plates, a vertical head-to-head block.",
        "ru": "Сравнительная таблица галочек и крестиков, плашки «было / стало», вертикальный баттл-блок."
      }
    },
    {
      "id": "pricing",
      "order": 7,
      "title": {
        "en": "Pricing and plans",
        "ru": "Прайс и тарифы"
      },
      "purpose": {
        "en": "An honest view of prices, what each package holds and how payment works.",
        "ru": "Прозрачная демонстрация цен, состава пакетов и условий оплаты."
      },
      "variants": {
        "en": "Plan cards with one marked \"popular\", a monthly/yearly switch, a price calculator.",
        "ru": "Карточки тарифов с выделенным «популярным», переключатель помесячной и годовой оплаты, калькулятор."
      }
    },
    {
      "id": "testimonials",
      "order": 8,
      "title": {
        "en": "Testimonials and social proof",
        "ru": "Отзывы и социальное доказательство"
      },
      "purpose": {
        "en": "Confirming quality through the real experience of other people.",
        "ru": "Подтверждение качества через реальный опыт других пользователей."
      },
      "variants": {
        "en": "A slider of quotes with photos, a grid of social screenshots, video testimonials, rating cards.",
        "ru": "Слайдер с цитатами и фото, сетка скриншотов из соцсетей, видео-отзывы, карточки с рейтингом."
      }
    },
    {
      "id": "showcase",
      "order": 9,
      "title": {
        "en": "Cases and portfolio",
        "ru": "Кейсы и портфолио"
      },
      "purpose": {
        "en": "Measurable results of the work, or the look of what was made.",
        "ru": "Демонстрация измеримых результатов работы или эстетики выполненных проектов."
      },
      "variants": {
        "en": "A filterable grid, \"task — solution — result in %\" cards, before/after sliders.",
        "ru": "Сетка с фильтрацией по категориям, карточки «задача — решение — результат в %», слайдеры «до и после»."
      }
    },
    {
      "id": "trust",
      "order": 10,
      "title": {
        "en": "Trust and logos",
        "ru": "Доверие и логотипы"
      },
      "purpose": {
        "en": "Reading the scale and reliability of the brand at a glance.",
        "ru": "Быстрое считывание масштаба и надёжности бренда."
      },
      "variants": {
        "en": "A marquee or grid of client and press logos, plates with key metrics, certificates.",
        "ru": "Бегущая строка или сетка логотипов клиентов и СМИ, плашки с ключевыми метриками, сертификаты."
      }
    },
    {
      "id": "page-material",
      "order": 11,
      "title": {
        "en": "Page material",
        "ru": "Материал страницы"
      },
      "purpose": {
        "en": "What a page is written FROM: paragraphs, lists, quotes, tables, code, callouts.",
        "ru": "То, ИЗ ЧЕГО написана страница: абзацы, списки, цитаты, таблицы, код, врезки."
      },
      "variants": {
        "en": "Not a landing section at all — building material that lives inside any of the ten above.",
        "ru": "Не лендинговая секция вовсе — материал, который живёт внутри любого из десяти типов выше."
      }
    },
    {
      "id": "workspace",
      "order": 12,
      "title": {
        "en": "Workspace",
        "ru": "Рабочий экран"
      },
      "purpose": {
        "en": "A working screen rather than a page to read: a menu on the left, content on the right. This is what dashboards and project tools are built from.",
        "ru": "Рабочий экран, а не страница для чтения: меню слева, содержимое справа. Из него строятся дашборды и инструменты проекта."
      },
      "variants": {
        "en": "Without a top row of sections (menu, heading, coloured cards, content) or with one, when the right-hand side itself splits into sections.",
        "ru": "Без верхнего ряда разделов (меню, заголовок, цветные карточки, содержимое) или с ним, когда правая часть сама делится на разделы."
      }
    },
    {
      "id": "charts",
      "order": 13,
      "title": {
        "en": "Charts",
        "ru": "Диаграммы"
      },
      "purpose": {
        "en": "Numbers shown as a picture: how a value moved over time, how a whole splits into parts, how several measures compare at once.",
        "ru": "Числа, показанные картинкой: как величина менялась во времени, как целое делится на части, как несколько мер сравниваются разом."
      },
      "variants": {
        "en": "Area, bars and line over a time series; pie and radial bars for shares; a radar for several measures at once; a tooltip-first example.",
        "ru": "Область, столбцы и линия по ряду времени; круговая и радиальная для долей; лепестковая для нескольких мер сразу; образец, где главное — подсказка."
      }
    }
  ],
  "kinds": [
    {
      "id": "0012",
      "codes": [
        "p01"
      ],
      "kind": "p",
      "type": "page-material",
      "shape": "text",
      "fields": "text: string",
      "title": null,
      "description": null,
      "hasCard": false,
      "usedOn": [
        {
          "page": "(home)",
          "order": 9,
          "times": 5
        },
        {
          "page": "accessibility",
          "order": 1,
          "times": 3
        },
        {
          "page": "agi-item",
          "order": 3,
          "times": 16
        },
        {
          "page": "cookies",
          "order": 2,
          "times": 2
        },
        {
          "page": "core",
          "order": 1,
          "times": 6
        },
        {
          "page": "host",
          "order": 1,
          "times": 1
        },
        {
          "page": "m2m",
          "order": 1,
          "times": 11
        },
        {
          "page": "privacy",
          "order": 2,
          "times": 2
        },
        {
          "page": "terms",
          "order": 2,
          "times": 2
        },
        {
          "page": "web3",
          "order": 3,
          "times": 16
        }
      ]
    },
    {
      "id": "0013",
      "codes": [
        "h201"
      ],
      "kind": "h2",
      "type": "page-material",
      "shape": "heading",
      "fields": "text: string",
      "title": null,
      "description": null,
      "hasCard": false,
      "usedOn": [
        {
          "page": "agi-item",
          "order": 2,
          "times": 2
        },
        {
          "page": "cookies",
          "order": 1,
          "times": 1
        },
        {
          "page": "host",
          "order": 2,
          "times": 2
        },
        {
          "page": "privacy",
          "order": 1,
          "times": 1
        },
        {
          "page": "terms",
          "order": 1,
          "times": 1
        },
        {
          "page": "web3",
          "order": 2,
          "times": 2
        }
      ]
    },
    {
      "id": "0014",
      "codes": [
        "h301"
      ],
      "kind": "h3",
      "type": "page-material",
      "shape": "heading",
      "fields": "text: string; id?: string",
      "title": null,
      "description": null,
      "hasCard": false,
      "usedOn": [
        {
          "page": "(home)",
          "order": 8,
          "times": 2
        },
        {
          "page": "agi-item",
          "order": 26,
          "times": 6
        },
        {
          "page": "core",
          "order": 9,
          "times": 2
        },
        {
          "page": "m2m",
          "order": 5,
          "times": 10
        },
        {
          "page": "web3",
          "order": 26,
          "times": 6
        }
      ]
    },
    {
      "id": "0033",
      "codes": [
        "h401"
      ],
      "kind": "h4",
      "type": "page-material",
      "shape": "heading",
      "fields": "text: string",
      "title": null,
      "description": null,
      "hasCard": false,
      "usedOn": [
        {
          "page": "agi-item",
          "order": 10,
          "times": 3
        },
        {
          "page": "web3",
          "order": 10,
          "times": 3
        }
      ]
    },
    {
      "id": "0034",
      "codes": [
        "h501"
      ],
      "kind": "h5",
      "type": "page-material",
      "shape": "heading",
      "fields": "text: string",
      "title": null,
      "description": null,
      "hasCard": false,
      "usedOn": []
    },
    {
      "id": "0015",
      "codes": [
        "quote01"
      ],
      "kind": "quote",
      "type": "page-material",
      "shape": "quote",
      "fields": "text: string; cite?: string; lead?: string",
      "title": null,
      "description": null,
      "hasCard": false,
      "usedOn": [
        {
          "page": "(home)",
          "order": 22,
          "times": 1
        },
        {
          "page": "agi-item",
          "order": 4,
          "times": 1
        },
        {
          "page": "core",
          "order": 23,
          "times": 1
        },
        {
          "page": "web3",
          "order": 4,
          "times": 1
        }
      ]
    },
    {
      "id": "0016",
      "codes": [
        "list01"
      ],
      "kind": "list",
      "type": "page-material",
      "shape": "list",
      "fields": "items: string[]",
      "title": null,
      "description": null,
      "hasCard": false,
      "usedOn": [
        {
          "page": "agi-item",
          "order": 12,
          "times": 1
        },
        {
          "page": "host",
          "order": 3,
          "times": 1
        },
        {
          "page": "web3",
          "order": 12,
          "times": 1
        }
      ]
    },
    {
      "id": "0017",
      "codes": [
        "olist01"
      ],
      "kind": "olist",
      "type": "page-material",
      "shape": "list",
      "fields": "items: string[]",
      "title": null,
      "description": null,
      "hasCard": false,
      "usedOn": [
        {
          "page": "(home)",
          "order": 10,
          "times": 2
        },
        {
          "page": "core",
          "order": 11,
          "times": 2
        },
        {
          "page": "host",
          "order": 5,
          "times": 1
        }
      ]
    },
    {
      "id": "0018",
      "codes": [
        "figure01"
      ],
      "kind": "figure",
      "type": "page-material",
      "shape": "figure",
      "fields": "media: 'image' | 'video'; src: string; alt: string; caption?: string; href?: string",
      "title": "картинка или видео с подписью",
      "description": "Иллюстрация в теле страницы. Показывает и файл проекта, и файл из хранилища — на экране разницы нет.\n\n## Правило владельца\n\n**Два пути к картинке, и оба существуют намеренно** (2026-08-13).\n\n| Форма `src` | Откуда файл | Кто его меняет |\n|---|---|---|\n| `media:<файл>` | **хранилище** проекта | владелец в панели — материал при этом не трогают вовсе |\n| обычный путь | файл проекта | правится вместе с текстом страницы |\n\n🔒 **Материал выбирает вид ССЫЛКИ, а не способ показа.** На экране оба пути дают одно и то же:\nте же размеры, ту же подложку, ту же оптимизацию. Разница только в том, кто будет менять картинку\nпотом — и потому выбор делается один раз, при написании материала.\n\nИменно эту развилку обязан повторить агент, читающий наш пример: увидев `media:`, не переписывать её\nв путь файла ради «единообразия» — это отнимет у владельца возможность заменить картинку без правки\nкода.\n\n## Когда не брать\n\nКартинка — часть уникальной раскладки, которую нельзя описать подписью и рамкой: тогда это **виджет**\nсвоего маршрута, а не блок каталога.",
      "hasCard": true,
      "usedOn": []
    },
    {
      "id": "0019",
      "codes": [
        "code01"
      ],
      "kind": "code",
      "type": "page-material",
      "shape": "code",
      "fields": "text: string",
      "title": null,
      "description": null,
      "hasCard": false,
      "usedOn": []
    },
    {
      "id": "0020",
      "codes": [
        "note01"
      ],
      "kind": "note",
      "type": "page-material",
      "shape": "callout",
      "fields": "text: string",
      "title": null,
      "description": null,
      "hasCard": false,
      "usedOn": [
        {
          "page": "host",
          "order": 6,
          "times": 1
        }
      ]
    },
    {
      "id": "0029",
      "codes": [
        "cta01"
      ],
      "kind": "cta",
      "type": "page-material",
      "shape": "cta",
      "fields": "text?: string; href: string; label: string; secondary?: { href: string; label: string }",
      "title": "призыв к действию",
      "description": "Кнопка там, где читателю предлагают следующий шаг. Одна на все такие места:\n`sections/cta-button.server.tsx`.\n\n## Правила владельца\n\nТри замечания, каждое сделано по живой странице и каждое меняет вид блока.\n\n**Подпись над кнопкой необязательна** (2026-08-16). Когда кнопка стоит внутри раздела, чей заголовок\nуже сказал то же самое, подпись превращается в дословный повтор через полэкрана.\n\n**Нет подписи — нет и рамки** (2026-08-19). Рамка существует, чтобы держать подпись; без неё она\nдержала пустоту: кнопка жалась влево, а справа тянулся пустой прямоугольник во всю ширину. Владелец\nназвал это «некрасиво», и он прав по существу — **контейнер без содержимого читается как поломка\nвёрстки, а не как воздух**.\n\n**Кнопка стоит по центру** (2026-08-20). Прижатая влево, она читается как продолжение абзаца, а не как\nприглашение: взгляд, дойдя до конца текста, уходит вниз по левому краю и минует её. Центр —\nединственное место, где кнопка принадлежит себе, а не предыдущей строке. Касается обеих раскладок: и\nвнутри рамки с подписью, и отдельно стоящей.\n\n## Когда не брать\n\nКнопка ведёт наружу или на страницу, которой нет, — это не `cta`, а ошибка: сторож ссылок\n(`check:links`) откажет. Нужна кнопка внутри рабочего экрана, а не в материале страницы, — её место в\nостровке, а не в блоке.",
      "hasCard": true,
      "usedOn": [
        {
          "page": "host",
          "order": 7,
          "times": 1
        },
        {
          "page": "m2m",
          "order": 40,
          "times": 1
        }
      ]
    },
    {
      "id": "0021",
      "codes": [
        "callout01"
      ],
      "kind": "callout",
      "type": "page-material",
      "shape": "callout",
      "fields": "title: string; text: string",
      "title": null,
      "description": null,
      "hasCard": false,
      "usedOn": []
    },
    {
      "id": "0022",
      "codes": [
        "table01"
      ],
      "kind": "table",
      "type": "page-material",
      "shape": "table",
      "fields": "headers: string[]; rows: string[][]; caption?: string",
      "title": null,
      "description": null,
      "hasCard": false,
      "usedOn": []
    },
    {
      "id": "0023",
      "codes": [
        "docref01"
      ],
      "kind": "docref",
      "type": "page-material",
      "shape": "docref",
      "fields": "title: string; summary: string; href: string; label?: string; kicker?: string",
      "title": null,
      "description": null,
      "hasCard": false,
      "usedOn": []
    },
    {
      "id": "0008",
      "codes": [
        "founder01"
      ],
      "kind": "founder",
      "type": "testimonials",
      "shape": "quote",
      "fields": "text: string",
      "title": "the owner's quote, signed from settings",
      "description": "**Type:** Testimonials and social proof (8). The only kind in this type today, and it carries ONE voice —\nthe owner's, not a customer's.\n\n## What it actually draws\n\nA pull quote with the signature under it: name, role and photo taken from the project settings, not from\nthe content.\n\n🔒 **The drawing itself moved to `sections/pull-quote.server.tsx`** (2026-08-16). The same look was\nneeded by a second block — a product rule with no author — and a copy of the gradient would have drifted\nfrom the original on the first theme edit. What stayed here is the only thing that belongs to this kind:\nTHE SIGNATURE.\n\n🔒 **Three absolute colours were removed earlier** (step 508, caught by a gate during the move):\n`text-gray-400` / `text-gray-500` in the signature and a violet gradient written straight into `style`.\nAll of them looked identical in both themes by construction — the same defect that left the blog black\nunder the light theme.\n\n## When to take it\n\nOne statement the owner stands behind personally, on a page where a person matters more than an\nargument.\n\n## When NOT to take it\n\n- Several testimonials from different people: no kind does that yet. Say so — do not repeat `founder`\n  three times with different names, the signature comes from settings and would be the same every time.\n- A quote from a document or an article — that is `quote`.",
      "hasCard": true,
      "usedOn": []
    },
    {
      "id": "0024",
      "codes": [
        "columns01"
      ],
      "kind": "columns",
      "type": "page-material",
      "shape": "two-columns",
      "fields": "children: Block[]; cols?: 2 | 3",
      "title": null,
      "description": null,
      "hasCard": false,
      "usedOn": [
        {
          "page": "agi-item",
          "order": 14,
          "times": 1
        },
        {
          "page": "web3",
          "order": 14,
          "times": 1
        }
      ]
    },
    {
      "id": "0025",
      "codes": [
        "group01"
      ],
      "kind": "group",
      "type": "page-material",
      "shape": "text",
      "fields": "children: Block[]",
      "title": null,
      "description": null,
      "hasCard": false,
      "usedOn": [
        {
          "page": "agi-item",
          "order": 15,
          "times": 2
        },
        {
          "page": "web3",
          "order": 15,
          "times": 2
        }
      ]
    },
    {
      "id": "0001",
      "codes": [
        "heroBadge01"
      ],
      "kind": "heroBadge",
      "type": "hero",
      "shape": "hero",
      "fields": "pill?: string",
      "title": "the mark and the eyebrow above the title",
      "description": "**Type:** Hero (1). The quietest of the two hero kinds: it carries no words of its own beyond one short\nlabel.\n\n## What it actually draws\n\nThe project logo, centred, and an optional eyebrow line under it. That is all.\n\n🔒 **It does NOT draw the page title.** The H1 is printed by the page factory — the same one that serves\nposts, legal pages and the home page. Putting a title here would produce a second H1 on the page.\n\n🔒 **The logo comes from settings, not from content.** Every project has its own and it changes in the\npanel without a rebuild. Nothing uploaded — no logo is drawn, and that is honest: somebody else's mark\nis worse than none.\n\n## When to take it\n\nThe first screen of a page whose title is set by the page itself, and where you want the mark and a\nshort label above it. Neither logo nor eyebrow — the section renders nothing at all, so do not use it\nas a spacer.\n\n## When NOT to take it\n\n- You need a real landing first screen with words and a picture side by side — that is `heroSplit`.\n- You want a big statement — that is `statement`.",
      "hasCard": true,
      "usedOn": []
    },
    {
      "id": "0002",
      "codes": [
        "heroSplit01"
      ],
      "kind": "heroSplit",
      "type": "hero",
      "shape": "hero-split",
      "fields": "title: string; description: string; pill?: string; image: 'homePage' | 'agiNode'; imageAlt: string; diagram?: { core: string; ring: [string, string, string, string]; peers: string }; mark?: boolean; cta?: { href: string; label: string; secondary?: { href: string; label: string } }",
      "title": "the landing first screen: words left, picture right",
      "description": "**Type:** Hero (1). The only kind meant to open a landing page.\n\n## What it actually draws\n\nMark and eyebrow centred; under them two columns — the heading with its text and button on the left, an\nillustration on the right.\n\n🔒 **This is the ONE kind that carries the H1 itself.** Everywhere else the title is printed by the page\nfactory. Here it has to stand inside the left column, and outside the grid it cannot get there. A page\nusing this section declares `titleInBody`, and the factory stops printing its own — otherwise the page\ngets two H1s.\n\n🔒 **The picture names a SETTINGS SLOT, not a file.** Every project has its own image and changes it in\nthe panel without a rebuild. Hard-coding a path here would ship one picture to every customer.\n\n🔒 **Its own width limit: 56rem, and it is a meaning decision** (owner, 2026-08-15). The section spans\nthe full screen and ignores the width toggle, but its CONTENT must not stretch: on a 2500px monitor a\nheading and a picture pushed to opposite edges read as a broken page, not a wide one.\n\n## When to take it\n\nThe first screen of a landing page — the one where the offer has to be readable in three seconds.\n\n## When NOT to take it\n\n- On a post, a legal page or a catalogue: they get their title from the factory, and this section would\n  fight it.\n- More than once on a site. Two different first screens teach the visitor that the site has no front\n  door.",
      "hasCard": true,
      "usedOn": [
        {
          "page": "(home)",
          "order": 1,
          "times": 1
        },
        {
          "page": "agi-item",
          "order": 1,
          "times": 1
        },
        {
          "page": "core",
          "order": 2,
          "times": 1
        },
        {
          "page": "web3",
          "order": 1,
          "times": 1
        }
      ]
    },
    {
      "id": "0004",
      "codes": [
        "badges01"
      ],
      "kind": "badges",
      "type": "benefits",
      "shape": "badges",
      "fields": "items: BadgeItem[]",
      "title": "a row of capability labels",
      "description": "**Type:** Benefits and value (2). The shortest way to say \"it can do all of this\" without a paragraph.\n\n## What it actually draws\n\nLabels wrapped into a centred row. Each has a `tone`, and the tone is what makes the row work.\n\n🔒 **Colour here WORKS, it does not decorate.** Eleven words split into four groups the eye reads before\nit reads: data, reach, access, code. Give every label the same tone and the row becomes a pile.\n\n🔒 **The tone map lives in `sections/tone.ts`, not in this renderer** (2026-08-16). It used to live\nhere, and a second kind (`noBill`) started showing labels too — a copy would have drifted on the first\nedit, and \"data\" on one row would be a different colour from \"data\" on the row below.\n\n**Label size comes from the type scale (`--fs-eyebrow`), not from a Tailwind step** — otherwise it stops\nmoving when the owner changes `--type-scale` in the panel.\n\n## When to take it\n\nA dense list of capabilities where each item is two or three words. Grouping matters more than detail.\n\n## When NOT to take it\n\n- Items need explaining — that is `cards`.\n- Items are numbers — that is `metrics`.\n- There are two or three of them: a row of three labels looks like something failed to load.",
      "hasCard": true,
      "usedOn": [
        {
          "page": "(home)",
          "order": 4,
          "times": 1
        },
        {
          "page": "agi-item",
          "order": 5,
          "times": 1
        },
        {
          "page": "core",
          "order": 5,
          "times": 1
        },
        {
          "page": "web3",
          "order": 5,
          "times": 1
        }
      ]
    },
    {
      "id": "0026",
      "codes": [
        "panel01"
      ],
      "kind": "panel",
      "type": "page-material",
      "shape": "panel",
      "fields": "tone?: 'plain' | 'warn' | 'accent'; eyebrow?: string; title: string; children: Block[]",
      "title": null,
      "description": null,
      "hasCard": false,
      "usedOn": [
        {
          "page": "agi-item",
          "order": 7,
          "times": 1
        },
        {
          "page": "web3",
          "order": 7,
          "times": 1
        }
      ]
    },
    {
      "id": "0009",
      "codes": [
        "metrics01"
      ],
      "kind": "metrics",
      "type": "trust",
      "shape": "numbers",
      "fields": "items: { value: string; label: string }[]",
      "title": "the numbers that prove it",
      "description": "**Type:** Trust and logos (10). Answers the reader's \"is that actually true?\" with a short number\ninstead of a paragraph.\n\n## 🔒 Exactly THREE items\n\nThe renderer is `sm:grid-cols-3` plus `divide-x` — three columns with dividers between them, from the\nsmall screen up. **On a phone there is one column**, and any number of items falls into place there —\nthe defect lives only on a wide screen, which is exactly where the owner looks at his page.\n\n**What breaks on the fourth.** It drops to a second row ALONE, into the left column, with an empty\nthird beside it and a divider hanging in the air. It reads not as \"one more metric\" but as a page that\ngave up. The owner called it, word for word, \"жуткое уродство\" (2026-08-21) — and he was right: a grid\nwith dividers promises symmetry, and the fourth element cancels that promise without giving anything\nback.\n\n**Two items** leave an empty third on the right. Formally fine, visibly short.\n\nThe type does not guard this yet: `items` is declared as an unbounded array\n(`lib/content/blocks/types.ts`), so a fourth element compiles in silence. Until that changes — count\nwith your eyes.\n\n## When to take it\n\nYou have **three** numbers, each of which the reader can check or at least measure against experience:\nhow many times over, how many minutes, how many people, since which year.\n\n## When NOT to take it\n\n- One number — that is `statement` or a callout, not a grid with one column filled.\n- Many numbers, all alike — that is `table`: a table promises no symmetry and therefore breaks none.\n- The \"numbers\" are really properties (\"fast\", \"reliable\", \"cheap\") — that is `badges`.\n- `metrics` already stands on this page: a kind is not repeated on one page (owner's rule, 2026-08-22).\n  A second proof is given by a different shape — `quote`, `table`, `flow`.\n\n## Owner's rules\n\n- **2026-08-21 — three and only three.** Occasion: reusing the section, the agent added a fourth column\n  and it landed against the left edge. Written down instead of a silent fix, so the next agent does not\n  repeat it.",
      "hasCard": true,
      "usedOn": [
        {
          "page": "(home)",
          "order": 3,
          "times": 1
        },
        {
          "page": "core",
          "order": 4,
          "times": 1
        },
        {
          "page": "m2m",
          "order": 2,
          "times": 1
        }
      ]
    },
    {
      "id": "0005",
      "codes": [
        "flow01"
      ],
      "kind": "flow",
      "type": "how-it-works",
      "shape": "steps",
      "fields": "badge?: string; title: string; note?: string; steps: [FlowStep, FlowStep, FlowStep] }; | { kind: 'personaCases'; badge?: string; title: string; note?: string; personas: [Persona, Persona, Persona] }; | { kind: 'statement'; text: string }; | { kind: 'invite'; href: string; label: string }; | {; title?: string; note?: string; cols?: 2 | 3; items: { title: string; text: string; href?: string; linkLabel?: string }[]",
      "title": "how it works, step by step",
      "description": "**Type:** How it works (3). The only kind in this type today.\n\n## What it actually draws\n\nA titled section with steps that light up in order, and light travelling along the link between them.\nEach step has its own title and text.\n\n🔒 **The heading uses the `content` variant, and that is a corrected mistake** (2026-08-16, spotted by\nthe owner at first glance). It stood as `variant=\"ui\"`. The typography primitive splits variants by the\nMEANING of the surface: `content` is the storefront — home page, posts, legal pages, catalogue; `ui` is\nwork screens. The home page is a storefront, so there is nothing to argue about.\n\nThe cost of that mistake was visible to the eye: two standalone sections stood side by side with\nDIFFERENT headings — one sans at 18px, the next serif at 24px. Exactly the mismatch the primitive\nexists to prevent.\n\n## When to take it\n\nA process with an order: first this, then that. Three steps read best; more than five and the section\nturns into a list that happens to be drawn as cards.\n\n## When NOT to take it\n\n- The pieces are equal and have no order — that is `cards`. Cards promise no sequence.\n- It is one process shown in two states, before and after — that is `problemSolution`.",
      "hasCard": true,
      "usedOn": [
        {
          "page": "(home)",
          "order": 5,
          "times": 1
        },
        {
          "page": "agi-item",
          "order": 6,
          "times": 3
        },
        {
          "page": "core",
          "order": 6,
          "times": 1
        },
        {
          "page": "web3",
          "order": 6,
          "times": 3
        }
      ]
    },
    {
      "id": null,
      "codes": [
        "personaCases01"
      ],
      "kind": "personaCases",
      "type": "page-material",
      "shape": "text",
      "fields": "badge?: string; title: string; note?: string; personas: [Persona, Persona, Persona] }; | { kind: 'statement'; text: string }; | { kind: 'invite'; href: string; label: string }; | {; title?: string; note?: string; cols?: 2 | 3; items: { title: string; text: string; href?: string; linkLabel?: string }[]",
      "title": "one deal, told three times",
      "description": "**Type:** How it works (3). Its neighbours are `flow` and `problemSolution`.\n\n## What it actually draws\n\nA titled section with a row of three person cards on top — a round avatar on the left, the name and\nthe role on the right — and, underneath, the six cases of whoever is selected. Picking a person\nchanges the container: the card gains a border, a tinted background and a glow.\n\n🔒 **Switching is radio inputs plus CSS, not an island.** All eighteen cases sit in the markup, so a\nsearch engine reads them and a person with JavaScript off still sees the whole story. An island would\ngive the opposite: the text would travel to the browser and the section would be empty without a\nscript. The rules live in `styles/globals.css` (`.pc-*`) — a `:checked ~` relation belongs to the\nlink between elements and cannot be written as a utility on one of them.\n\n🔒 **Exactly three people, exactly six cases each.** This is not a list of people: it is one story\ntold from three sides. A fourth person would turn a scenario into a catalogue, and the type refuses\nit.\n\n## When to take it\n\nOne process that touches several parties, where the interesting part is that the same events look\ndifferent to each of them: a client, a provider and a validator in one deal.\n\n## When NOT to take it\n\n- A process with an order and no second point of view — that is `flow`, and it takes exactly three\n  steps (the owner's rule, 2026-09-21).\n- Two states of one process, before and after — that is `problemSolution`.\n- Equal pieces with no order and no people — that is `cards`.",
      "hasCard": true,
      "usedOn": [
        {
          "page": "m2m",
          "order": 13,
          "times": 1
        }
      ]
    },
    {
      "id": "0006",
      "codes": [
        "problemSolution01"
      ],
      "kind": "problemSolution",
      "type": "comparison",
      "shape": "two-columns",
      "fields": "badge?: string; title: string; note?: string; demandLabel: string; answerLabel: string; items: { title: string; demand: string; answer: string }[] }; | { kind: 'flow'; badge?: string; title: string; note?: string; steps: [FlowStep, FlowStep, FlowStep] }; | { kind: 'personaCases'; badge?: string; title: string; note?: string; personas: [Persona, Persona, Persona] }; | { kind: 'statement'; text: string }; | { kind: 'invite'; href: string; label: string }; | {; title?: string; note?: string; cols?: 2 | 3; items: { title: string; text: string; href?: string; linkLabel?: string }[]",
      "title": "cases on the left, the chosen one broken down on the right",
      "description": "**Type:** Comparison (6). The only kind in this type today.\n\n## What it actually draws\n\nTwo halves: a list of cases on the left, and on the right the one selected — \"what you do\" on top,\n\"why it works here\" below.\n\n🔒 **Not a single line of script, and that is the main decision of this kind.** The example it was taken\nfrom switches tabs with React state: without JavaScript there is not one case on the page, and the\ncrawler gets a separate invisible copy of the text — an admission that the visible version does not\nserve it. Here the switching is done by the browser itself: the list is `radio` inputs, the labels are\ntheir labels, and showing the right half is a `:checked` rule in `styles/globals.css`.\n\nWhat that buys at once: the text of ALL cases sits in the server markup; a person without scripts uses\nthe section fully, not \"tolerably\"; the keyboard works because it is a real form control.\n\n## When to take it\n\nTwo things set against each other — old way versus new, us versus them, before versus after. Or several\ncases where the visitor picks the one that is his.\n\n## When NOT to take it\n\n- The pieces are not opposed, they simply differ — that is `cards`.\n- It is a real table of ticks and crosses — that is `table`.\n- There is only one case: the left column becomes a list of one, and the section looks broken.",
      "hasCard": true,
      "usedOn": [
        {
          "page": "(home)",
          "order": 24,
          "times": 1
        },
        {
          "page": "core",
          "order": 25,
          "times": 1
        },
        {
          "page": "m2m",
          "order": 14,
          "times": 1
        }
      ]
    },
    {
      "id": "0003",
      "codes": [
        "cards01"
      ],
      "kind": "cards",
      "type": "benefits",
      "shape": "cards",
      "fields": "badge?: string; title: string; note?: string; cols?: 2 | 3; children: Block[]",
      "title": "a section made of cards",
      "description": "**Type:** Benefits and value (2). Answers \"what is this made of\" or \"what are my options\" — with\nseveral equal pieces the reader chooses between or compares.\n\n## The number of cards is a multiple of the columns\n\n`cols` takes **2 or 3** (default 3), and the grid does not reflow by width: it is `md:grid-cols-2` or\n`md:grid-cols-3`.\n\n**What breaks.** A card count that is not a multiple of the column count leaves the last row\nincomplete — one card under three, pushed to the left. It is the same defect `metrics` has, and the\nowner was burned by it twice in one day (2026-08-21): first with numbers, then with cards.\n\nSo the working pairs are: **3 columns — 3, 6, 9 cards**; **2 columns — 2, 4, 6**. Four cards in three\ncolumns is the most common mistake, because \"four\" feels like a natural number.\n\n**A fifth card in three columns is legal in exactly one case:** when the bottom row of two reads as a\npair by meaning, not as a remainder. That is a judgement, not a rule.\n\n## When to take it\n\nThree or more equal pieces, each with its own heading and its own content. Anything can live inside a\ncard — a paragraph, a list, a subheading: a card is a container, not a line.\n\n## When NOT to take it\n\n- The pieces are NOT equal, one matters more — that is `panel` or `callout`.\n- There is an order between them, the first precedes the second — that is `flow`: cards promise no\n  sequence.\n- There are exactly two and they are opposed — `problemSolution` or `columns`.\n- They are numbers, not stories — that is `metrics`.\n- `cards` already stands on this page: a kind is not repeated (owner's rule, 2026-08-22).\n\n## Owner's rules\n\n- **2026-08-21 — never add a card beyond the multiple.** Occasion: reusing the section, the agent added\n  a fourth card to three, and the last row fell apart.",
      "hasCard": true,
      "usedOn": [
        {
          "page": "(home)",
          "order": 6,
          "times": 2
        },
        {
          "page": "agi-item",
          "order": 24,
          "times": 2
        },
        {
          "page": "core",
          "order": 7,
          "times": 2
        },
        {
          "page": "m2m",
          "order": 3,
          "times": 3
        },
        {
          "page": "web3",
          "order": 24,
          "times": 2
        }
      ]
    },
    {
      "id": "0027",
      "codes": [
        "card01"
      ],
      "kind": "card",
      "type": "page-material",
      "shape": "panel",
      "fields": "tone?: Tone; children: Block[]",
      "title": null,
      "description": null,
      "hasCard": false,
      "usedOn": [
        {
          "page": "(home)",
          "order": 7,
          "times": 5
        },
        {
          "page": "agi-item",
          "order": 25,
          "times": 6
        },
        {
          "page": "core",
          "order": 8,
          "times": 5
        },
        {
          "page": "m2m",
          "order": 4,
          "times": 10
        },
        {
          "page": "web3",
          "order": 25,
          "times": 6
        }
      ]
    },
    {
      "id": "0028",
      "codes": [
        "statement01"
      ],
      "kind": "statement",
      "type": "page-material",
      "shape": "statement",
      "fields": "text: string",
      "title": "крупное утверждение в разрядку",
      "description": "То, ради чего написан раздел: одна фраза, набранная так, что её читают до чтения остального.\n\n## Правило: чем отличается от цитаты владельца\n\n`statement` и `founder` рисуются **одним и тем же** компонентом (`sections/pull-quote.server.tsx`) —\nзначит разойтись между собой они не могут. Различает их ровно одно: **наличие подписи**.\n\n| Вид | Подпись | Что помечает |\n|---|---|---|\n| `founder` | имя и фотография **из настроек проекта** | слова ЧЕЛОВЕКА |\n| `statement` | нет | правило, которое установил ПРОДУКТ |\n\n🔒 **РАЗЛИЧИЕ ПО СУЩЕСТВУ, А НЕ ЭКОНОМИЯ РАЗМЕТКИ.** Правило продукта — например, «пока хоть один\nкейс не подтверждён, агент отказывается строить» — человек не произносил, его устанавливает продукт.\nПодпись под таким текстом **приписала бы владельцу фразу, которой он не говорил**, причём выглядело бы\nэто совершенно убедительно: имя, фотография, кавычки. Ложное авторство тем опаснее, чем аккуратнее\nсвёрстано.\n\n## Когда не брать\n\nФраза принадлежит человеку и он готов под ней подписаться — это `founder`, и подпись обязательна.\nУтверждение нуждается в пояснении рядом — это `callout` или абзац: разрядка существует для короткой\nмысли, длинную она делает нечитаемой.",
      "hasCard": true,
      "usedOn": [
        {
          "page": "agi-item",
          "order": 22,
          "times": 2
        },
        {
          "page": "web3",
          "order": 22,
          "times": 2
        }
      ]
    },
    {
      "id": "0055",
      "codes": [
        "invite01"
      ],
      "kind": "invite",
      "type": "page-material",
      "shape": "invite",
      "fields": "href: string; label: string",
      "title": "приглашение в пунктирной рамке",
      "description": "Место, где чего-то ещё нет, и это можно занять. Вся карточка — ссылка.\n\n## Чем отличается от `cta`, и почему это не оформление\n\n`cta` — сплошная кнопка: «сделайте следующий шаг», она про **готовое** действие, которое уже можно\nсовершить. Пунктир говорит человеку обратное: **здесь пока пусто**. Этим приёмом живут все\nредакторы, и объяснять его словами не приходится — незаполненная рамка узнаётся сразу.\n\nОтсюда правило выбора: ведёте к тому, что уже есть, — берите `cta`. Показываете место, которое ещё\nпредстоит занять, — берите `invite`.\n\n## Правила владельца\n\n**Карточка обязана стоять в контейнере содержимого, сразу после последнего предложения, во всю ширину\nс отступами страницы** (2026-08-31, дословно: «предполагалось, что это красивая пунктирная карточка,\nкоторая будет стоять в контейнере, где находится контент… занимает всю ширину экрана с отступами»).\n✗ Оплачено шагом 70: слот, из которого её рисовали, стоял ЗА колонкой текста, и карточка теряла\nширину ленты и боковые отступы.\n\n**Вид не изобретается заново там, где он уже есть** (та же дата, дословно: «переиспользуй только\nкомпонент, который у нас существует»). ✗ Оплачено тем же шагом: вместо этой карточки была нарисована\nсвоя кнопка — сплошная, фирменного цвета, со своим значком.\n\n## Рисунок общий, природа разная\n\nКласс живёт в `lib/ui/dashed-card.ts` и читается двумя потребителями:\n\n| Где | Чем является | Что делает |\n|---|---|---|\n| страницы подвала, на заглушке | **кнопка** (клиентский островок) | открывает окно заявки |\n| этот вид каталога | **ссылка** (серверный рендерер) | ведёт по адресу |\n\n🔒 Одинаковыми они обязаны быть **только на вид**. Скопируй класс — и две пунктирные карточки\nразойдутся на первой правке темы, причём заметит это тот, кто откроет обе подряд, то есть никто.\n\n## Когда не брать\n\nСсылка ведёт наружу или на несуществующую страницу — `check:links` откажет, и правильно сделает:\nприглашение занять место, ведущее в никуда, хуже отсутствующего.\nНужна кнопка внутри рабочего экрана, а не в материале страницы, — её место в островке, а не в блоке.",
      "hasCard": true,
      "usedOn": []
    },
    {
      "id": "0007",
      "codes": [
        "noBill01"
      ],
      "kind": "noBill",
      "type": "pricing",
      "shape": "statement",
      "fields": "badge?: string; heading: string; note?: string; items: { vendor: string; text: string; badge: BadgeItem }[]; title: string; text: string; cta?: { page: 'm2m' }",
      "title": "the invoices that will not come",
      "description": "**Type:** Pricing and plans (7). The only kind in this type today, and it argues about price from the\nother side: not what you pay, but what you stop paying.\n\n## What it actually draws\n\nA named section: an H2 above, a list of other companies' names struck through with what you no longer\nbuy from them, and an H3 conclusion below.\n\n🔒 **Two headings of different levels, and it is not a repetition.** The H2 on top NAMES the section:\nwithout it the section stood in the middle of the page nameless — there were arguments, but the page did\nnot say whose. The H3 below is the CONCLUSION, and it has to come after the list: three struck-through\nnames are the evidence, the conclusion follows from them. Put the conclusion on top and the reader gets\na verdict before its grounds — that is asking him to take your word.\n\n🔒 **The NAME is struck through, not the whole phrase.** A struck-through line \"you do not pay Vercel\"\nreads as a cancelled statement — the exact opposite of what is meant.\n\n## When to take it\n\nThe pricing part of a landing page where the argument is savings, not a price list.\n\n## When NOT to take it\n\n- You need plans with prices and a \"popular\" one: no kind does that yet — say so instead of bending\n  this one.\n- The list is your own features, not somebody else's invoices — that is `cards` or `badges`.",
      "hasCard": true,
      "usedOn": [
        {
          "page": "(home)",
          "order": 23,
          "times": 1
        },
        {
          "page": "core",
          "order": 24,
          "times": 1
        }
      ]
    },
    {
      "id": "0030",
      "codes": [
        "faq01"
      ],
      "kind": "faq",
      "type": "page-material",
      "shape": "faq",
      "fields": "title?: string; items: FaqPair[]",
      "title": "questions and answers, last on the page",
      "description": "**Type:** Page material (11). Not a landing section: it can close any page — a post, a legal page, the\nhome page — and it is the only kind a search engine reads as pairs of \"term and definition\".\n\n## Where the words come from\n\nThe **heading is printed by the mechanism**, not by the material. \"Frequently asked questions\" already\nexists in ten languages in `lib/content/page-ui.ts`, so the block carries no heading of its own.\n`title` overrides it, and it is meant for a page whose questions are about one specific thing rather\nthan frequent — not for translating the default, which would put ten copies of one word into ten\nlanguage cells.\n\nThe **pairs come from the page's own `faq` field**, the same field that feeds the `FAQPage` structured\ndata. Step 542 moved the drawing into this kind and changed nothing about where the content is\ndeclared: one place to write questions, one place to draw them.\n\n## No inline markup, and that is deliberate\n\nAlmost every neighbouring kind resolves `**bold**` and `[links](…)`. This one does not. The very same\nstrings are handed to search engines as `FAQPage` JSON-LD, and structured data is taken **verbatim** —\nasterisks and brackets would be printed to the crawler as part of the answer. An answer that genuinely\nneeds a link is not an FAQ answer; it is a section of prose with a heading of its own.\n\n## One per page\n\nThe anchor is fixed (`#faq-heading`) so that it can be linked to from outside the page. Two of these\nsections would emit the same `id` twice, which breaks both the link and the markup. This is the\ngeneral owner's rule — a kind is not repeated on a page (2026-08-22) — with a technical reason on top.\n\n## When to take it\n\nReal questions a reader asks before acting: what it costs, what happens next, what if it goes wrong.\nThree to seven pairs. The section closes the page: only the back link sits below it.\n\n## When NOT to take it\n\n- The \"questions\" are headings in disguise, and the answers are paragraphs — that is `h2` plus `p`,\n  and it belongs in the body where the table of contents can see it.\n- One question with a long answer — that is `callout` or `panel`.\n- Answers that need links, bold text or lists — see the structured-data rule above.\n- Two independent sets of questions on one page — see the anchor rule above.\n\n## What it draws\n\nA section separated by a rule above it, the heading, and then one bordered plate per pair: the\nquestion in solid text, the answer in muted text beneath it. Markup is `<dl>` / `<dt>` / `<dd>`,\nbecause the pairing is the content of the section, not its decoration.",
      "hasCard": true,
      "usedOn": []
    },
    {
      "id": "0031",
      "codes": [
        "toc01"
      ],
      "kind": "toc",
      "type": "page-material",
      "shape": "toc",
      "fields": "items: TocItem[]",
      "title": "the table of contents of a page",
      "description": "**Type:** Page material (11). Built by the page factory, never by the author — the one kind of the\ncatalogue whose content is computed rather than written.\n\n## You do not fill it in\n\n`items` are derived from the `h2` blocks of the body, with the same `headingId` the headings\nthemselves print. Two consequences follow, and both are the reason the kind works at all:\n\n- **The anchors match by construction.** A second way of turning a heading into an address diverges\n  on the first text with punctuation, and then the contents lead nowhere — a defect nobody notices,\n  because the link still scrolls, just to the wrong place or to none.\n- **The words cannot go stale.** Written by hand, the list becomes a second copy of the headings. Fix\n  a heading, forget the copy, and the page keeps a working link with a wrong label on it.\n\n**Decision of the owner, 2026-08-22: the table of contents stays automatic.** It appears whenever the\nbody has `h2` headings, and no page declares it. That is exactly how it behaved before step 542; what\nchanged is only who draws it — a catalogue kind instead of layout written inside the factory.\n\n## What it draws\n\nA bordered plate above the body: an uppercase lead line — the mechanism's own words (\"On this page\")\nplus the number of entries — then a numbered list of links. The number is a **sign, not text**:\n`aria-hidden` keeps it out of screen readers, where \"01\" before a section name says nothing and gets\nin the way. Its contrast was raised to full `muted-foreground` after an accessibility check\n(2026-08-13): at `/70` the ratio fell below the threshold.\n\nAn empty list draws nothing at all, rather than an empty plate.\n\n## Only `h2` gets in\n\n`h3` does not, and that is a rule of the page rather than of this kind: a table of contents of two\nlevels stops being scannable at about seven entries, which is where most pages already are. A section\nthat deserves its own line in the contents deserves an `h2`.\n\n## Known debt\n\n`aria-label=\"Contents\"` is in English in every language. It was carried over verbatim in step 542 so\nthat the move stayed a move; it is fixed together with the rest of the mechanism's labels, not here.\n\n## When NOT to take it\n\n- Placing it by hand to move it elsewhere on the page — the factory adds its own, and you would get\n  two.\n- A list of links to OTHER pages — that is `list` with links, or a section of cards. This kind means\n  \"the sections of this page\" and its anchors point inside the page.",
      "hasCard": true,
      "usedOn": []
    },
    {
      "id": "0010",
      "codes": [
        "languageMarquee01"
      ],
      "kind": "languageMarquee",
      "type": "trust",
      "shape": "marquee",
      "fields": "title: string; note?: string",
      "title": "the language ribbon, closing the page",
      "description": "**Type:** Trust and logos (10). An outro section: it ends a page, it does not open one.\n\n## What it actually draws\n\nA ribbon of languages crawling left to right — flag and the language name written in that language,\nlonger than the screen can hold.\n\n🔒 **What it proves.** Eighty-two languages is a claim that means nothing as a list in a sentence. The\nribbon shows them in the face, one after another. That is also what makes it an example of a section\nthat needs the full screen width.\n\n🔒 **ALL 82, not the enabled set.** Two languages are on today, ten tomorrow — the ribbon is about what\nthe product can do, not about the current setting. Hence `ALL_LANGUAGE_METADATA`, not\n`SUPPORTED_LANGUAGES`.\n\n🔒 **The movement is CSS, without a line of JS.** The page must work with scripts off, and a ribbon that\nonly exists after hydration is a ribbon the crawler never sees.\n\n## When to take it\n\nThe very bottom of a page where breadth of reach is the argument.\n\n## When NOT to take it\n\n- In the middle of a page: it takes the full width and cuts the reading in half.\n- Twice on one site. It is a closing gesture; a second one turns it into wallpaper.",
      "hasCard": true,
      "usedOn": [
        {
          "page": "(home)",
          "order": 25,
          "times": 1
        },
        {
          "page": "core",
          "order": 26,
          "times": 1
        }
      ]
    },
    {
      "id": "0011",
      "codes": [
        "projectTypeMarquee01"
      ],
      "kind": "projectTypeMarquee",
      "type": "trust",
      "shape": "marquee",
      "fields": "title?: string; note?: string",
      "title": "the ribbon of directions",
      "description": "**Type:** Trust and logos (10). Deliberately the same breed of section as the language ribbon.\n\n## What it actually draws\n\nA ribbon of twenty-two cards naming what can be built on this product — from a landing page to a company\nbrain — each with one line saying how it differs from its neighbour.\n\n🔒 **What it proves.** \"Build anything\" is a claim that means nothing. The ribbon shows the specifics in\nthe face.\n\n🔒 **The directions come from the CATALOGUE, not from page content.** The list lives in\n`config/project-types.ts` and is the same one the owner picks his project structure from in the panel. A\nsecond copy inside a language cell would drift from the panel on the first direction added.\n\n## When to take it\n\nA page that has to answer \"what is this even for\" with breadth rather than depth.\n\n## When NOT to take it\n\n- Right next to `languageMarquee`: two ribbons in a row read as one long ribbon, and both stop proving\n  anything.\n- When the visitor already knows what he is building — then breadth is noise.",
      "hasCard": true,
      "usedOn": [
        {
          "page": "(home)",
          "order": 2,
          "times": 1
        },
        {
          "page": "core",
          "order": 3,
          "times": 1
        }
      ]
    },
    {
      "id": "0032",
      "codes": [
        "voiceField01"
      ],
      "kind": "voiceField",
      "type": "page-material",
      "shape": "text",
      "fields": "variant?: 'line' | 'area'; title: string; hint?: string; comment?: string; placeholder?: string",
      "title": "a text field that can be dictated",
      "description": "**Type:** Page material (11). The first kind of the catalogue that TAKES something from a visitor\ninstead of showing them something. Everything below follows from that one difference.\n\n## Two sizes, one kind\n\n`variant: 'line'` is a single-line input with the microphone built into the same frame; `variant:\n'area'` is a text area with a full-width microphone button underneath. The default is `line` — a\nshort answer is needed more often than a long one.\n\nThey are the same field, which is why they are one kind and not two. Two kinds would mean two cards,\ntwo taxonomy entries and two registry lines that must change in pairs — and the first edit to one of\nthem would drift from the other silently.\n\n## It has no receiver, and that is a decision\n\n**Owner's decision, 2026-08-28**, chosen out of two named options: the dictated text lives in the\nbrowser and disappears on reload. The block renders the field and works by voice; where the text goes\nis separate work with its own table, its own door and its own spam protection.\n\nSo: this kind is **not a contact form**. It does not submit, it does not store, it does not email.\nPutting it on a public page and expecting to read the answers later is the one way to be wrong about\nit. When the receiver is built, it arrives as fields on this same kind, not as a second kind.\n\n## What it draws\n\n`H3` title, a hint underneath, the field with its microphone, and — under the whole area — the\ncomment. While recording, an equaliser bar appears below the field; when recording stops, the bar is\n**replaced in the same place** by the transcript, which the visitor edits and then accepts or\ndiscards. One state at a time: showing the bar and the transcript together would claim a person is\nspeaking and correcting what they said at once.\n\nThree refusals get their own line rather than a button tooltip: no API key, the browser denied the\nmicrophone, the connection is not secure. A tooltip does not exist on a phone.\n\n## What it needs to actually transcribe\n\nHTTPS, a signed-in session and an OpenAI key on the server — the door is `/api/transcribe` and it\nchecks the role. Without them the field still works as an ordinary text field and says why the\nmicrophone is unavailable. That is deliberate: a control that silently does nothing teaches nobody.\n\n## When NOT to take it\n\n- **As a comment form or a lead form.** It has no receiver — see above.\n- **Inside an article, to look interactive.** It is form material: it belongs on a page that asks the\n  visitor for something, next to the rest of that form.\n- **For a value you already have.** The field starts empty and owns its own state; it is not bound to\n  settings, to a product or to a record.",
      "hasCard": true,
      "usedOn": []
    },
    {
      "id": "0035",
      "codes": [
        "workspace01",
        "workspace02"
      ],
      "kind": "workspace",
      "type": "workspace",
      "shape": "two-columns",
      "fields": "menuTitle?: string; menu: WorkspaceItem[]; title: string; lead?: string; notes?: WorkspaceNote[]; tabs?: WorkspaceItem[]; children: Block[]; widget?: import('react').ReactNode",
      "title": "рабочий экран",
      "description": "Меню слева, содержимое справа. Из него строятся дашборды, инструменты проекта и\nслужебные разделы — всё, что человек **использует**, а не читает.\n\n## Правила владельца\n\n**Решение 2026-08-30, дословно:** «я веду речь о том чтобы блок передавал интерфейс двух колоночного\nмакета в котором существует левая часть как меню, и правой часть контент… чтобы мы могли просто\nподключать эту сущность к проекту используя его параметры настраивать».\n\n**Два случая содержимого, названные им же:**\n\n1. заголовок · описание · три цветные карточки · содержимое проекта;\n2. то же самое **плюс верхний ряд разделов** — когда правая часть сама делится на разделы.\n\n🔒 **Это ОДИН вид, а не два.** Разница между случаями — поле `tabs`, а рисунок тот же. Второй вид\nразошёлся бы с первым на первой правке темы; тот же закон, по которому `quote` не разделилась надвое\nиз-за необязательной первой строки.\n\n## Три тона карточек различаются жанром, а не громкостью\n\n| Тон | Цвет | Что говорит |\n|---|---|---|\n| `recommended` | зелёный | **советует** — обязан выглядеть приглашением, а не предостережением |\n| `advice` | жёлтый | называет **цену решения** до того, как его приняли; остаётся верным всегда |\n| `warning` | красный | сообщает **состояние системы** сейчас; исчезнет, когда состояние изменится |\n\nДва одинаково окрашенных блока подряд человек читает как один длинный текст и второй пропускает.\n\n## Что легко сделать неправильно\n\n🔒 **Мёртвая ссылка хуже её отсутствия.** `href` у пункта необязателен: есть адрес — ссылка, нет —\nотмеченный текущий пункт. Проставить `href=\"#\"` всем пунктам ради «настоящего вида» значит обещать\nпереход, которого нет.\n\n🔒 **`active` ставится ровно одному пункту.** Экранный диктор читает `aria-current` как «текущая\nстраница»; два таких пункта означают, что человек находится в двух местах сразу.\n\n🔒 **Меню — навигация, а не содержание.** В машинной версии страницы оно не печатается намеренно:\nмодель, прочитав список из восьми пунктов, пересказала бы его как содержание экрана.\n\n🔒 **Раскладка снята с живого меню слоя архитектора.** До `md` — горизонтальная лента, с `md` —\nлипкая колонка `w-60`; разделитель едет вслед за меню. `self-start` у липкой колонки обязателен:\nбез него flex растягивает её на всю высоту строки и `sticky` **молча** перестаёт работать.\n\n## 🔒 Липкими держатся ОБА навигатора — меню слева и ряд разделов сверху\n\nС `md` левое меню и верхний ряд разделов прилипают к одной высоте. Причина названа владельцем\n2026-08-30: «когда лента в правой секции очень большая, механизмы навигации уходят далеко вверх, что\nделает невозможным навигацию между вкладками до того, как я верну страницу».\n\nПравка одна, а страниц три: ряд разделов рисует `components/workspace/workspace-shell.tsx`, и его же\nзовут обе страницы слоя архитектора. Липкий ряд обязан иметь фон — прозрачный пропускает под собой\nуезжающий текст.",
      "hasCard": true,
      "usedOn": []
    },
    {
      "id": null,
      "codes": [
        "accordion01"
      ],
      "kind": "accordion",
      "type": "page-material",
      "shape": "text",
      "fields": "title?: string; lead?: string; capped?: boolean; cards?: boolean; children: Block[]",
      "title": "раскрывающиеся полосы",
      "description": "Заголовок раздела и несколько свёрнутых полос: видно всё оглавление, читается выбранное. Внутрь\nполосы кладут любые блоки каталога — абзац, список, совет, ссылку.\n\n## Откуда взялся вид\n\n**Решение владельца 2026-09-19, дословно:** «Дело эту идею достаточно компактно при необходимости\nможешь использовать раскрывающуюся карточку типа аккордеон чтобы показать больше описание».\n\nЗадача, ради которой вид заведён: корневая страница слоя архитектора обязана объяснить десяток тем\nсразу. Простыню текста не читают, а сокращение до трёх строк теряет половину сказанного. Аккордеон —\nтретий ответ.\n\n## Когда брать и когда НЕ брать\n\n| Берут | Не берут |\n|---|---|\n| тем много, читать будут одну-две | тем две-три и обе короткие — обычные абзацы честнее |\n| у каждой темы своя начинка (список, совет, ссылка) | это вопросы и ответы — тогда `faq` |\n| текст важен, но не всем и не сразу | текст обязателен к прочтению — свёрнутое не читают вовсе |\n\n🔒 **Это НЕ `faq`, и различие не в оформлении.** У «вопросов и ответов» своя семантика: `<dl>` — пара\n«термин — определение», постоянный якорь `faq-heading`, один раздел на страницу и своё место в\nразметке для поисковика. Там разметка и есть содержание; взять её ради похожего вида значит соврать.\n\n## Устройство\n\n```\n{ kind: 'accordion', title?: string, lead?: string, capped?: boolean, cards?: boolean, children: Block[] }\n{ kind: 'accordionItem', summary: string, open?: boolean, children: Block[] }\n```\n\n🔒 **`cards: true` — каждая полоса отдельной карточкой с отступом** (279, слово владельца 2026-09-23:\n«одно решение одна карточка аккордеон за доступно открытие только одной карточки»). Для списка\nсамостоятельных сущностей — решений, товаров; без поля — прежний вид, одна рамка с разделителями.\nОткрытой по-прежнему бывает одна полоса.\n\n🔒 **Ячейка — контейнер, а не строка.** Довод оплачен у `cards`: внутрь кладут не только абзац.\nСтрока такого не выражает, и вид пришлось бы расширять полями «а ещё список».\n\n🔒 **`open` ставится не более чем одной полосе.** Раскрытые по умолчанию полосы перестают быть\nаккордеоном: человек получает ту же простыню, ради сворачивания которой вид и заведён. Рендерер берёт\nпервую помеченную и остальные помечает свёрнутыми.\n\n🔒 **Открыта одна полоса за раз** (`type=\"single\" collapsible`). Закрыть последнюю оставшуюся человек\nимеет право — иначе экран навсегда занят разделом, который он дочитал.\n\n## Что легко сделать неправильно\n\n🛑 **Положить внутрь `accordion` что-то кроме `accordionItem`.** Такой блок негде открыть, и он\nисчезнет с экрана без следа. Тип этого не даёт, а рендерер молча отбрасывает чужих детей — последняя\nлиния для материала, собранного не типами.\n\n🛑 **Спрятать сюда то, что человек обязан прочесть.** Свёрнутое не читают; предупреждение о том, что\nсайт открыт без авторизации, живёт в аккордеоне только потому, что его ПЕРВАЯ строка видна всегда.\n\n🔒 **Вид состоит из двух половин, и это не сложность ради сложности.** Раскрытие живёт движением, а ни\nодин файл под `sections/` не бывает клиентским. Поэтому рендерер серверный и только переводит блоки в\nпропсы островка `components/accordion/accordion-section.client.tsx` — тот же приём, что у `workspace`\nи у диаграмм. Содержимое приходит островку **уже нарисованным**: он не знает о видах и не заводит\nвторой системы разметки.",
      "hasCard": true,
      "usedOn": []
    },
    {
      "id": null,
      "codes": [
        "accordionItem01"
      ],
      "kind": "accordionItem",
      "type": "page-material",
      "shape": "text",
      "fields": "summary: string; open?: boolean; details?: { title: string; children: Block[] }; children: Block[]",
      "title": null,
      "description": null,
      "hasCard": false,
      "usedOn": []
    },
    {
      "id": "0036",
      "codes": [
        "benefitCards01"
      ],
      "kind": "benefitCards",
      "type": "benefits",
      "shape": "cards",
      "fields": "title?: string; note?: string; cols?: 2 | 3; items: { title: string; text: string; href?: string; linkLabel?: string }[]",
      "title": "карточки возможностей со ссылкой",
      "description": "Ряд ячеек: заголовок · черта · текст · ссылка внизу. Форма перенесена с главной витрины\n(шаг 52, 2026-08-30) — там ею показывают, из чего состоит продукт.\n\n## Чем отличается от `cards`, раз тот тоже рисует ряд ячеек\n\n| | `cards` | `benefitCards` |\n|---|---|---|\n| что внутри ячейки | **любые блоки** — это контейнер | закреплённый порядок: заголовок, черта, текст, ссылка |\n| кто решает порядок | автор материала | вид |\n| ссылка | обычный блок среди прочих | прижата к низу ячейки |\n\n🔒 **РАДИ ПОСЛЕДНЕЙ СТРОКИ ВИД И СУЩЕСТВУЕТ.** В ряду из трёх карточек текст всегда разной длины;\n`h-full` и `justify-between` ставят все ссылки на одну линию. Собранная из контейнеров, такая карточка\nвыравнивается только случайно — и расходится на первой же правке текста.\n\n## Правило владельца, которое здесь НЕ действует, и почему\n\n**Решение владельца 2026-08-20:** кнопка вида `cta` стоит **по центру** — прижатая влево, она читается\nкак продолжение абзаца, а не как приглашение.\n\n🔒 **Внутри карточки это правило не применяется, и это не обход.** У `cta` центр обязателен потому,\nчто кнопка принадлежит РАЗДЕЛУ и должна выделиться из текста вокруг. Здесь кнопки нет вовсе: есть\nтекстовая ссылка в углу ячейки, и она принадлежит СВОЕЙ ячейке. Поставленная по центру, она разорвала\nбы карточку пополам.\n\nЕсли понадобится кнопка под всем рядом — это `cta` отдельным блоком, и там центр обязателен.\n\n## Когда не брать\n\nЯчейки без ссылок — это `cards`: пустой угол там, где ждут ссылку, читается как недоделка.\nЯчеек больше шести — ряд перестаёт читаться, и это уже список.",
      "hasCard": true,
      "usedOn": [
        {
          "page": "m2m",
          "order": 28,
          "times": 2
        }
      ]
    },
    {
      "id": "0037",
      "codes": [
        "splitPair01"
      ],
      "kind": "splitPair",
      "type": "product-demo",
      "shape": "two-columns",
      "fields": "title?: string; note?: string; left: { image?: string; alt?: string; title: string; text: string }; right: { image?: string; alt?: string; title: string; text: string }",
      "title": null,
      "description": null,
      "hasCard": false,
      "usedOn": []
    },
    {
      "id": "0038",
      "codes": [
        "logoCards01"
      ],
      "kind": "logoCards",
      "type": "showcase",
      "shape": "cards",
      "fields": "title?: string; note?: string; items: { title: string; text: string; source?: string }[]",
      "title": null,
      "description": null,
      "hasCard": false,
      "usedOn": []
    },
    {
      "id": "0039",
      "codes": [
        "carousel01"
      ],
      "kind": "carousel",
      "type": "how-it-works",
      "shape": "figure",
      "fields": "title?: string; note?: string; slides: { image?: string; alt?: string; title: string; text?: string }[]",
      "title": null,
      "description": null,
      "hasCard": false,
      "usedOn": []
    },
    {
      "id": "0040",
      "codes": [
        "support01"
      ],
      "kind": "support",
      "type": "pricing",
      "shape": "cards",
      "fields": "badge?: string; title: string; body?: string[]; tiers: {; amount: string; period?: string; sublabel: string; badge?: string; perks: string[]; cta?: { href: string; label: string }; }[]; note?: string; link?: { label: string; text: string; href: string }",
      "title": "поддержка проекта: ряд тарифов со звёздочками",
      "description": "Ярлык рубрики, заголовок, один-два абзаца вступления. Ниже ряд из трёх: крупная сумма и период по\nодной базовой линии, подпись, перечень достоинств со звёздочкой, кнопка внизу. Один тариф несёт ярлык\nНАД карточкой и залит. Под рядом — строка условия и широкая ссылка на тех, кто уже поддерживает.\n\n## Откуда взялась форма\n\nПеренесена с нашей витрины — `fractera-easy-starter/components/sections/sponsorship-section.tsx`.\n\n✗ **ВИД ПЕРЕПИСАН 2026-08-30 ПО СЛОВУ ВЛАДЕЛЬЦА, дословно:** «support01 — абсолютно некачественная\nработа, удаляй это, заменяй на `sponsorship-section.tsx`». В шаге 52 вид был назван переносом витрины,\nа на деле собран по мотивам: три карточки с именем, суммой и абзацем. Не осталось ни звёздочек, ни\nвыделенного тарифа, ни кнопки, ни строки «кто уже поддерживает».\n\n🔒 **Урок тот же, что владелец назвал в шаге 53: название секции даётся затем, чтобы форму ВЗЯТЬ, а не\nпридумать похожую.**\n\n## Правила вида\n\n🔒 **Выделенный тариф ровно один, и выделяет его ЯРЛЫК.** Отдельного поля «выделить» нет намеренно:\nнельзя выделить тариф, не сказав, за что ему это. Два выделенных не выделяют ничего.\n\n🔒 **Кнопка ведёт по адресу.** У источника она открывает оплату; в каталоге оплаты нет, поэтому это\nссылка с `href`. Кнопка, которая никуда не ведёт, хуже её отсутствия — тот же закон, что у\n`priceTable`.\n\n🔒 **Это НЕ прайс-лист, и разница существенная.** У цены есть обязанность: заплатив, человек получает\nназванное. Поддержка — добровольный взнос; обещать за неё возможности значит превратить её в продажу\nбез гарантии. Соседний `priceTable` — про обязанность, этот вид — про добрую волю.\n\n## Три отличия от источника, каждое названо\n\n1. **Жёлтый витрины заменён токеном темы** (решение владельца о цветах, 2026-08-30): буквальная копия\n   палитры сломала бы тему каждого клиента.\n2. **Состояния приложения не переехали.** У источника есть экран «спасибо» после оплаты и подсказка\n   «сначала войдите» — они принадлежат сеансу и оплате, а не форме. Место второй занимает обычная\n   строка под рядом.\n3. **Подсветка карточки под курсором не перенесена.** В источнике её держит `useState`; здесь она была\n   бы островком ради одного пикселя сдвига.\n\n## Когда НЕ брать\n\n- Продают подписку с обязанностями — это `priceTable`.\n- Говорят о счетах, которых НЕ будет, — это `noBill`: он про экономию, этот про деньги, которые\n  человек отдаёт по своей воле.\n- Тариф один: ряд из одной карточки выглядит поломкой.",
      "hasCard": true,
      "usedOn": []
    },
    {
      "id": "0041",
      "codes": [
        "showcaseCarousel01"
      ],
      "kind": "showcaseCarousel",
      "type": "use-cases",
      "shape": "figure",
      "fields": "badge?: string; title?: string; note?: string; slides: {; image?: string; label: string; sublabel: string; title: string; description: string; }[]",
      "title": null,
      "description": null,
      "hasCard": false,
      "usedOn": []
    },
    {
      "id": "0042",
      "codes": [
        "featureGrid01"
      ],
      "kind": "featureGrid",
      "type": "benefits",
      "shape": "cards",
      "fields": "badge?: string; title?: string; note?: string; items: { icon?: FeatureIcon; title: string; label?: string; tone?: Tone; text: string }[]",
      "title": null,
      "description": null,
      "hasCard": false,
      "usedOn": []
    },
    {
      "id": "0043",
      "codes": [
        "promoBand01"
      ],
      "kind": "promoBand",
      "type": "trust",
      "shape": "two-columns",
      "fields": "title: string; text: string; cta?: { href: string; label: string; icon?: 'github' | 'link' }; image?: string; alt?: string",
      "title": "полоса с текстом слева и картинкой справа",
      "description": "Заголовок, абзац и кнопка со значком слева, изображение справа, акцентная черта в четыре пикселя\nсверху и снизу. На телефоне всё складывается в столбик.\n\n## Откуда взялась форма\n\nПеренесена с нашей витрины — `fractera-easy-starter/components/sections/fractera-promo.tsx`, шаг 55,\n2026-08-30.\n\n## 🔒 ГДЕ ЕЙ МЕСТО — ЭТО ЧАСТЬ ВИДА, А НЕ ВКУС\n\n**Полосу ставят в НИЖНЮЮ ЧАСТЬ страницы, которая сама растянута на всю ширину.** Тогда она читается\nтак же, как на витрине: широкая лента под содержимым, отбитая двумя чертами.\n\nСлова владельца, 2026-08-30, после того как он увидел её в каталоге блоков:\n\n> «Обрати внимание, что эта секция внутри нашей страницы ломает разметку. То есть она не находится\n> внутри своего собственного контейнера, а выходит наружу за его пределы… Если секция промобанд01\n> имеет в своих стилях специальный способ сломать вёрстку, растянуть на весь экран, то это\n> неправильно — ты просто в комментариях к этой секции укажи, что она должна располагаться в нижней\n> части страницы, которая растягивается на всю ширину.»\n\n## 🪦 Приём «выход за колонку» отменён 2026-08-30\n\nПолоса выходила за колонку страницы через `w-screen` плюс отрицательный отступ\n(`margin-left: calc(50% - 50vw)`), а корень держал защиту `overflow-x: clip`. **Ушло и то и другое.**\n\nДовод владельца — соседний вид: `languageMarquee` тоже занимает всю ширину и при этом **остаётся в\nсвоём контейнере**. Значит полная ширина не требует ломать колонку, и разница была не в задаче, а в\nприёме.\n\nЗащита снята вместе с приёмом намеренно: правило на корне маскировало бы любое ДРУГОЕ горизонтальное\nпереполнение, а такое переполнение — дефект, который лучше видеть.\n\n## Что осталось от источника без изменений\n\n🔒 **Фон полосы СОВПАДАЕТ с фоном страницы.** Отделяют её две черты и ширина. Залить полосу другим\nцветом значит превратить приём в обычную карточку, только пошире.\n\n🔒 **Значок кнопки выбирается именем из двух** — `github` или `link`. В источнике значок вшит разметкой\nSVG на тридцать строк: витрина знает, куда ведёт её единственная кнопка, каталог не знает.\n\n🔒 **Картинка необязательна, и без неё рисуется заглушка.** Исчезнув совсем, она роняет текст на всю\nширину полосы, и две колонки превращаются в одну.\n\n## Когда НЕ брать\n\n- Полоса нужна в середине текста — она перебивает чтение; там `callout` или `panel`.\n- Кнопки нет и не будет: без действия полоса становится картинкой с подписью — это `figure`.",
      "hasCard": true,
      "usedOn": []
    },
    {
      "id": "0044",
      "codes": [
        "priceTable01"
      ],
      "kind": "priceTable",
      "type": "pricing",
      "shape": "cards",
      "fields": "title: string; note?: string; periodLabels?: { monthly: string; yearly: string }; plans: {; name: string; monthlyPrice: string; yearlyPrice?: string; monthlyPeriod?: string; yearlyPeriod?: string; features: string[]; cta?: { href: string; label: string }; highlighted?: boolean; }[]",
      "title": "тарифы с переключателем периода",
      "description": "Заголовок и описание, переключатель «месяц / год», ряд карточек: ярлык с названием, крупная цена,\nподпись периода, черта, список возможностей с галочками, кнопка внизу. Выделенный тариф получает\nзаливку и сплошную кнопку.\n\n## Откуда взялась форма\n\nПрислана владельцем **из внешней библиотеки блоков** 2026-08-30 — не из нашей витрины, и это меняет\nобращение с источником.\n\n🔒 **ВИТРИНУ ПЕРЕНОСЯТ ОДИН В ОДИН, ЧУЖОЙ КОД ЧИТАЮТ КАК ОБРАЗЕЦ ФОРМЫ.** Витрина наша и проверена\nвременем: её решения оплачены нашими же ошибками. За чужой библиотекой такой истории нет, и всё, что\nв ней мертво, остаётся снаружи.\n\n## Что НЕ перенесено и почему\n\n✗ **Восемь полей источника объявлены и ни разу не отрисованы:** `image`, `icon` у плана, `tagline`,\n`bestFor`, `planCode`, `buttonUrl`, `highlightedLabel` и `icon` у возможности — для последнего даже\nнаписана функция-извлекатель, которую никто не зовёт.\n\nПеренести их значило бы завести в **закрытом** каталоге поля, которые ничего не делают: следующий\nагент честно заполнит `bestFor`, не увидит его на странице и потратит время на поиск несуществующей\nполомки.\n\n## Три отличия от источника, каждое — исправление\n\n🔒 **У кнопки есть адрес.** В источнике `buttonUrl` объявлен, а кнопка — `<Button>` без ссылки: нажать\nможно, уйти некуда. **Тариф, кнопка которого никуда не ведёт, хуже тарифа без кнопки.**\n\n🔒 **Переключатель — радиокнопка и правило CSS, а не островок.** Причин две. Обе цены лежат в разметке,\nскрыта лишь одна: поисковик читает **обе**, и годовая цена — та самая, которую сравнивают, — существует\nдля всех, а не только для браузера с работающими скриптами. Островок нарисовал бы текущую. Вторая\nпричина приземлённее: переключателем в источнике служит `Tabs`, а такого компонента в шаблоне нет.\n\n🔒 **Цена — заголовок из шкалы проекта, а не зашитый `text-5xl`.** Владелец двигает размер вместе со\nвсем набором; зашитое число выпало бы из шкалы при первой же правке.\n\n## Что легко сделать неправильно\n\n🔒 **Годовая цена необязательна, и её отсутствие убирает переключатель целиком.** Рычаг, у которого обе\nполовины показывают одно и то же, — обман: человек нажимает и решает, что интерфейс сломан.\n\n🔒 **Выделенный тариф ровно один.** Два выделенных не выделяют ничего.\n\n🔒 **Подпись периода прячется, но место занимает** (`invisible`, не удаление). У бесплатного тарифа\nпериода нет; убери подпись совсем — его цена поднимется на строку выше соседних, и ряд перестанет\nчитаться рядом. Тот же приём в источнике.\n\n**Предел — три-четыре тарифа.** Дальше карточки сужаются до нечитаемости, и это уже таблица сравнения,\nа не ряд.",
      "hasCard": true,
      "usedOn": []
    },
    {
      "id": "0045",
      "codes": [
        "spotlightPair01"
      ],
      "kind": "spotlightPair",
      "type": "comparison",
      "shape": "two-columns",
      "fields": "badge?: string; title?: string; note?: string; left: SpotlightHalf; right: SpotlightHalf",
      "title": "пара, между которой ходит подсветка",
      "description": "Две половины рядом: картинка, заголовок, черта с полосой отсчёта, абзац. Активная занимает 7/10\nширины, соседняя 3/10; через девять секунд внимание переходит на другую. На телефоне половины стоят\nдруг под другом, и движения там нет — при одинаковой ширине «широкая» и «узкая» не значат ничего.\n\n## Откуда взялась форма\n\nПеренесена с нашей витрины — `fractera-easy-starter/components/sections/double-presentation.tsx`,\n2026-08-30, по прямому указанию владельца: «next section … double-presentation.tsx type for\ncomparison». Тип назначен им же.\n\n🔒 **ЭТО ВТОРОЙ ЗАХОД В ТОТ ЖЕ ФАЙЛ ВИТРИНЫ, И ЭТО НЕ ДУБЛИРОВАНИЕ.** Шаг 52 взял оттуда `splitPair` —\nстатику пары. Здесь взято то, чего `splitPair` взять не мог.\n\n## Чем отличается от `splitPair`, раз рисунок похож\n\n**Поведением, а не рисунком, и в этом весь выбор между ними.**\n\n- **`splitPair`** — две равные половины, обе видны одинаково. Берут, когда показывают продукт с двух\n  сторон, равных по весу.\n- **`spotlightPair`** — половины неравны в каждый момент времени. Берут, когда две вещи\n  **противопоставлены** и читатель должен смотреть на одну за раз: старый способ против нового, мы\n  против них, до против после.\n\nОтсюда и разные типы каталога: `splitPair` — «продукт в действии», этот вид — «сравнение».\n\n## Правила владельца\n\n- **2026-08-22 (шаг 52), про карусель, и здесь оно действует так же:** движение, которое нельзя\n  остановить, отнимает право дочитать. В источнике остановки нет вовсе — там таймер и только. Здесь\n  каждая половина сама себе ярлык переключателя: **нажал — подсветка замерла на выбранной навсегда**.\n- **2026-08-30, цвета:** токены темы, а не фиолетовый витрины. Буквальная копия палитры сломала бы\n  тему каждого клиента.\n\n## Ни одной строки скрипта\n\nИсточник держит состояние на `useState` и двух таймерах. Здесь движение делает CSS-анимация, выбор —\nрадиокнопка, а **обе половины целиком лежат в серверной разметке**. Что это покупает: человек без\nJavaScript получает секцию полностью, поисковик читает оба текста, а не активный, и работает\nклавиатура — переключатель настоящий, а не нарисованный.\n\n🔒 **Запретили движение в системе — половины становятся равными, а не замирают на первой.** Замершая\nподсветка утверждала бы, что левая важнее, а источник этого не говорил.\n\n## Что легко сделать неправильно с самим движением\n\n✗ **Кадры, кончающиеся на втором состоянии, дают перескок.** Владелец поймал это 2026-08-30: переход\nот первой карточки ко второй шёл плавно, обратный — рывком. Цикл анимации не возвращается «по кругу»\nсам: последний кадр обязан вернуть карточку к исходной ширине, иначе между концом круга и его началом\nнет времени, а значит нет и перехода.\n\n## Когда НЕ брать\n\n- Половины не противопоставлены, а просто разные — это `splitPair` или `cards`.\n- Половин больше двух: форма держится на равновесии пары, третья ячейка превращает\n  противопоставление в ряд карточек.\n- Сравнивают по пунктам, галочками и крестиками, — это `table`, а не пара.\n- Нужны несколько случаев на выбор читателя — это `problemSolution`, второй вид этого же типа.\n\n## Что легко сделать неправильно\n\n🔒 **Половины, одинаковые по смыслу.** Подсветка, ходящая между двумя утверждениями об одном и том же,\nне сравнивает ничего и просто мигает.\n\n🔒 **Заголовок в одну строку.** Он обрезается многоточием намеренно — так в источнике: длинный\nзаголовок разорвал бы высоту половин, и пара перестала бы читаться парой.",
      "hasCard": true,
      "usedOn": []
    },
    {
      "id": "0046",
      "codes": [
        "platformGrid01"
      ],
      "kind": "platformGrid",
      "type": "benefits",
      "shape": "cards",
      "fields": "badge?: string; title: string; note?: string; cards: { title: string; subtitle: string; company?: string }[]; disclaimer?: string",
      "title": "сетка площадок, светящаяся из щелей",
      "description": "Ярлык рубрики, заголовок, описание. Ниже сетка: две колонки на телефоне, три на мониторе. В каждой\nячейке заголовок, короткое объяснение и имя компании внизу. Под сеткой — оговорка мелким шрифтом.\n\n## Откуда взялась форма\n\nПеренесена с нашей витрины — `fractera-easy-starter/components/sections/platforms-grid.tsx`,\n2026-08-30, по прямому указанию владельца: «final from FES will platforms-grid.tsx for type benefits».\nТип назначен им же.\n\n## 🔒 ГЛАВНОЕ В ФОРМЕ — ШОВ, А НЕ КАРТОЧКА\n\nЯчейки стоят вплотную, промежуток между ними ровно **два пикселя**, и в этот промежуток светит\nподложка сетки: радиальная заливка акцентным цветом от центра. Ни рамок, ни теней — решётка ярче в\nсередине и гаснет к краям.\n\nОтсюда следствие, которое легко испортить: **ячейка обязана быть непрозрачной**. Прозрачный фон\nзаливает подложкой всю ячейку, и приём исчезает — остаётся цветное пятно.\n\n## 🔒 ЧИСЛО ЯЧЕЕК КРАТНО ШЕСТИ\n\nКолонок две на телефоне и три на мониторе. Кратное шести — единственное, что заполняет **обе**\nраскладки без обрубка: пять ячеек оставят дыру в одной из двух, и увидит её тот, кто смотрит с другого\nустройства, то есть не автор. Тот же закон уже оплачен дважды — у `cards` и `metrics`.\n\n## Чем отличается от `featureGrid`, раз оба живут в «Преимуществах»\n\n- **`featureGrid`** перечисляет **умения продукта**: значок, заголовок, ярлык, объяснение.\n- **`platformGrid`** перечисляет **чужие площадки** — отсюда строка компании у каждой ячейки и\n  оговорка о торговых марках под сеткой.\n\n## Подсветка: шесть направлений, как в образце\n\nВладелец перечислил их 2026-08-30 поимённо, и они перенесены все:\n\n| Ячейка | Откуда светит |\n|---|---|\n| верх слева | правый нижний угол |\n| верх центр | понизу |\n| верх справа | левый нижний угол |\n| низ слева | правый верхний угол |\n| низ центр | поверху |\n| низ справа | левый верхний угол |\n\nСмысл один: **подсветка каждой ячейки смотрит в центр сетки.**\n\n✗ **Оплачено в тот же день.** Сначала я перенёс только горизонтальную составляющую, объяснив это тем,\nчто число строк разметке неизвестно. Неизвестно число строк — но известны первая\n(`nth-child(-n+N)`) и последняя (`nth-last-child(-n+N)`), где N — число колонок, и этого достаточно.\nЯчейка по-прежнему не знает своего номера: направления назначает CSS, а не разметка, поэтому седьмая\nи двенадцатая ячейки ведут себя осмысленно — средние строки светят из середины.\n\n## Одно отличие от источника\n\n**Цвет — токен темы, а не фиолетовый витрины** (решение владельца о цветах, 2026-08-30).\n\n## Когда НЕ брать\n\n- Перечисляются свои возможности, а не чужие продукты, — это `featureGrid`.\n- Нужны логотипы, а не тексты, — это `logoCards`.\n- Ячеек три или четыре: сетка перестаёт читаться решёткой, и это обычные `cards`.",
      "hasCard": true,
      "usedOn": []
    },
    {
      "id": "0047",
      "codes": [
        "chartArea01"
      ],
      "kind": "chartArea",
      "type": "charts",
      "shape": "chart",
      "fields": "title: string; description?: string; rows?: ChartRow[]; labels?: { a?: string; b?: string }; ranges?: ChartRange[]",
      "title": "область: как менялось целое и из чего оно состояло",
      "description": "Карточка с заголовком, описанием и выпадающим списком отрезков в шапке. Ниже — две области с\nнакоплением по оси времени: сглаженные линии, заливка градиентом, сетка без вертикальных линий,\nлегенда снизу. Наведение показывает подсказку с точкой и обоими значениями дня.\n\n## Откуда взялась форма\n\nПеренесена с `https://ui.shadcn.com/charts/bar#charts`, образец «Area Chart — Interactive», 2026-08-30,\nпо прямому указанию владельца: «в нашей группе блоков остаётся последний блок… он будет называться\nдиаграммы, мы создадим в нём по одной диаграмме, и я тебе пришлю их исходный код». Исходник лежит в\nучёте шага: `development-docs/development-steps/new-steps/58/sources/01-area-interactive.tsx`.\n\n## 🔒 ЦВЕТ РЯДА — РОЛЬ ПАЛИТРЫ, А НЕ ЗНАЧЕНИЕ В КОДЕ\n\nРешение владельца 2026-08-30, дословно: «цвета диаграмм — часть палитры дизайна». Ряды берут\n`var(--chart-1)` и `var(--chart-2)`; умолчание живёт в теме, а человек правит их на\n`/{lang}/architect/design?section=colors` — как фирменный цвет. Пересборка не нужна.\n\nОтсюда следствие: **записать цвет ряда прямо в блок нельзя.** Такой цвет переживёт смену палитры и\nоднажды окажется единственным пятном старого оформления на перекрашенном сайте.\n\n## ✗ ПЕРВЫЙ РЯД ОДНАЖДЫ БЫЛ ПОЧТИ НЕВИДИМ\n\nУмолчания взяты из палитры источника, и там первым стоит самое светлое значение. На белом фоне\nобласть с градиентом от 0.8 к 0.1 превратилась в дымку: диаграмма рисовалась, данные были на месте,\nа увидеть их было нельзя. Поймано глазами в браузере — ни один счётчик разметки этого не показывает.\nСветлая шкала темы развёрнута: те же пять значений, первым самый тёмный.\n\n## 🔒 ВИД СОСТОИТ ИЗ ДВУХ ПОЛОВИН, И ЭТО НЕ СЛОЖНОСТЬ РАДИ СЛОЖНОСТИ\n\nДиаграмма живёт движением: выбор отрезка, подсказка под курсором. Ни один файл под `sections/` не\nбывает клиентским — это свойство слоя. Поэтому рендерер остаётся серверным и только переводит поля\nблока в пропсы островка `components/charts/chart-area.client.tsx`. Тот же приём, что у `workspace`.\n\n## 🔒 ДАННЫЕ НЕОБЯЗАТЕЛЬНЫ, И БЕЗ НИХ ВИД РИСУЕТ ОБРАЗЕЦ\n\n`rows` можно не давать: тогда берётся `VISITORS_90D` — тот самый массив из исходника владельца,\nдевяносто одна точка. Пустая карточка на витрине читается как поломка вида, а не как отсутствие\nматериала.\n\nСвоя строка выглядит так: `{ x: '2024-04-01', a: 222, b: 150 }`. Имена нейтральные намеренно — в\nисточнике ряды зовутся `desktop`/`mobile`, но это подписи ЕГО примера; как назвать ряды на экране,\nрешает `labels`.\n\n## 🔒 ОТСЧЁТ ИДЁТ ОТ ПОСЛЕДНЕЙ ТОЧКИ ДАННЫХ\n\nВ источнике дата отсчёта зашита числом (`2024-06-30`) — это дата его примера. Здесь «последние 7\nдней» считаются от последней строки `rows`, иначе на любых других данных список молча показывал бы\nпустоту.\n\n## Когда НЕ брать\n\n- Нужно показать доли одного момента — это `chartPie` или `chartRadial`.\n- Ряд один и без накопления, важна форма кривой — это `chartLine`.\n- Значения дискретны и сравниваются по столбикам — это `chartBar`.\n- Данных меньше десятка точек: область на них выглядит случайной кляксой.",
      "hasCard": true,
      "usedOn": []
    },
    {
      "id": "0048",
      "codes": [
        "chartBar01"
      ],
      "kind": "chartBar",
      "type": "charts",
      "shape": "chart",
      "fields": "title: string; description?: string; rows?: ChartRow[]; labels?: { a?: string; b?: string }",
      "title": "столбцы: сравнить величины поточечно",
      "description": "Карточка, у которой вместо заголовка — две кнопки с итогами рядов. Нажатая кнопка меняет ряд, и\nдиаграмма рисует столбцы за девяносто один день. Подсказка показывает дату полностью, с годом.\n\n## 🔒 ИТОГ В ШАПКЕ СЧИТАЕТСЯ, А НЕ ЗАПИСЫВАЕТСЯ\n\nСумма ряда берётся из самих строк. Поля для итога нет и не будет: записанное число разойдётся с\nданными на первой правке и разойдётся молча — сумму девяноста чисел никто не проверяет глазами.\n\n## 🔒 РИСУЕТСЯ ОДИН РЯД, И ЭТО РЕШЕНИЕ ИСТОЧНИКА\n\nДва ряда столбиками на длинном отрезке времени сливаются. Источник решает это переключателем;\nесли нужны оба ряда сразу — это `chartArea` с накоплением, а не столбцы.\n\n## Когда НЕ брать\n\n- Нужна форма кривой, а не высота отдельных значений, — это `chartLine`.\n- Нужны оба ряда одновременно — это `chartArea`.\n- Точек меньше десяти и главное в них — подсказка: это `chartTooltip`.\n\n## Откуда взялась форма\n\nПеренесена с `https://ui.shadcn.com/charts/bar#charts` 2026-08-30 по прямому указанию владельца:\n«мы создадим в нём по одной диаграмме, и я тебе пришлю их исходный код». Исходник лежит в учёте\nшага 58, папка `new-steps/58/sources/`.\n\n## 🔒 ЦВЕТ РЯДА — РОЛЬ ПАЛИТРЫ, А НЕ ЗНАЧЕНИЕ В КОДЕ\n\nРешение владельца 2026-08-30, дословно: «цвета диаграмм — часть палитры дизайна». Ряды берут\n`var(--chart-1)` … `var(--chart-5)`; умолчание живёт в теме, человек правит их на\n`/{lang}/architect/design?section=colors`, и правка видна без пересборки. Записать цвет прямо в\nблок нельзя: он пережил бы смену палитры и остался единственным пятном старого оформления.\n\n## 🔒 ВИД СОСТОИТ ИЗ ДВУХ ПОЛОВИН\n\nНи один файл под `sections/` не бывает клиентским. Рендерер остаётся серверным и переводит поля\nблока в пропсы островка `components/charts/chart-bar.client.tsx`, где и живёт движение.\n\n## 🔒 ДАННЫЕ НЕОБЯЗАТЕЛЬНЫ\n\nНе прислали своих — рисуется `VISITORS_90D` из `components/charts/sample-data.ts`, тот самый массив\nиз исходника владельца. Пустая карточка на витрине читается как поломка вида.",
      "hasCard": true,
      "usedOn": []
    },
    {
      "id": "0049",
      "codes": [
        "chartLine01"
      ],
      "kind": "chartLine",
      "type": "charts",
      "shape": "chart",
      "fields": "title: string; description?: string; rows?: ChartRow[]; labels?: { a?: string; b?: string }",
      "title": "линия: увидеть форму движения",
      "description": "Та же шапка с двумя итогами, что у `chartBar`, и линия вместо столбцов: сглаживание `monotone`,\nтолщина 2, без точек.\n\n## 🔒 ТОЧЕК НА ЛИНИИ НЕТ, И ЭТО НЕ УПРОЩЕНИЕ\n\nДевяносто одна точка на ширине карточки сливается в пунктир: линия перестаёт читаться линией.\nТочки возвращают только на коротких рядах, и это уже другой вид.\n\n## Чем отличается от `chartBar`, раз шапка та же\n\nПредметом. Столбцы отвечают «сколько было в этот день», линия — «как оно шло». Различия формы\nперенесены из источника целиком, включая отступы карточки и порядок цветов рядов.\n\n## Когда НЕ брать\n\n- Сравниваются отдельные значения по высоте — это `chartBar`.\n- Нужны оба ряда сразу и важна их сумма — это `chartArea`.\n\n## Откуда взялась форма\n\nПеренесена с `https://ui.shadcn.com/charts/bar#charts` 2026-08-30 по прямому указанию владельца:\n«мы создадим в нём по одной диаграмме, и я тебе пришлю их исходный код». Исходник лежит в учёте\nшага 58, папка `new-steps/58/sources/`.\n\n## 🔒 ЦВЕТ РЯДА — РОЛЬ ПАЛИТРЫ, А НЕ ЗНАЧЕНИЕ В КОДЕ\n\nРешение владельца 2026-08-30, дословно: «цвета диаграмм — часть палитры дизайна». Ряды берут\n`var(--chart-1)` … `var(--chart-5)`; умолчание живёт в теме, человек правит их на\n`/{lang}/architect/design?section=colors`, и правка видна без пересборки. Записать цвет прямо в\nблок нельзя: он пережил бы смену палитры и остался единственным пятном старого оформления.\n\n## 🔒 ВИД СОСТОИТ ИЗ ДВУХ ПОЛОВИН\n\nНи один файл под `sections/` не бывает клиентским. Рендерер остаётся серверным и переводит поля\nблока в пропсы островка `components/charts/chart-line.client.tsx`, где и живёт движение.\n\n## 🔒 ДАННЫЕ НЕОБЯЗАТЕЛЬНЫ\n\nНе прислали своих — рисуется `VISITORS_90D` из `components/charts/sample-data.ts`, тот самый массив\nиз исходника владельца. Пустая карточка на витрине читается как поломка вида.",
      "hasCard": true,
      "usedOn": []
    },
    {
      "id": "0050",
      "codes": [
        "chartPie01"
      ],
      "kind": "chartPie",
      "type": "charts",
      "shape": "chart",
      "fields": "title: string; description?: string; shares?: ChartShareRow[]; footer?: { note?: string; hint?: string }",
      "title": "круговая: из чего состоит целое",
      "description": "Квадратная область, подписи долей прямо на секторах, подсказка без строки заголовка, две строки\nподвала под диаграммой.\n\n## 🔒 ПЯТЬ ДОЛЕЙ — ПРЕДЕЛ, И ОН ВЫВЕДЕН ИЗ ПАЛИТРЫ\n\nРолей цвета диаграммы ровно пять (`--chart-1` … `--chart-5`). Шестая доля осталась бы без своей\nпеременной и нарисовалась бы цветом по умолчанию — чужим для проекта. Вид берёт первые пять и\nостальное не рисует; сводить хвост в «Прочее» — работа материала, как и в образце.\n\n## 🔒 ЦВЕТ КАЖДОГО СЕКТОРА ПРИХОДИТ ИЗ ПАЛИТРЫ «ДИЗАЙНА»\n\nДоля получает `fill: var(--color-<имя>)`, а имена объявлены в конфигурации диаграммы, которую\n`ChartStyle` превращает в переменные. Сменил человек `chart-4` на своей странице «Дизайна» —\nсменился четвёртый сектор, без пересборки.\n\n## Когда НЕ брать\n\n- Показывается изменение во времени — это `chartArea`, `chartBar` или `chartLine`.\n- Долей больше пяти или они близки по величине: секторы перестают различаться.\n- Нужна шкала, по которой видно абсолютную величину, — это `chartRadial`.\n\n## Откуда взялась форма\n\nПеренесена с `https://ui.shadcn.com/charts/bar#charts` 2026-08-30 по прямому указанию владельца:\n«мы создадим в нём по одной диаграмме, и я тебе пришлю их исходный код». Исходник лежит в учёте\nшага 58, папка `new-steps/58/sources/`.\n\n## 🔒 ЦВЕТ РЯДА — РОЛЬ ПАЛИТРЫ, А НЕ ЗНАЧЕНИЕ В КОДЕ\n\nРешение владельца 2026-08-30, дословно: «цвета диаграмм — часть палитры дизайна». Ряды берут\n`var(--chart-1)` … `var(--chart-5)`; умолчание живёт в теме, человек правит их на\n`/{lang}/architect/design?section=colors`, и правка видна без пересборки. Записать цвет прямо в\nблок нельзя: он пережил бы смену палитры и остался единственным пятном старого оформления.\n\n## 🔒 ВИД СОСТОИТ ИЗ ДВУХ ПОЛОВИН\n\nНи один файл под `sections/` не бывает клиентским. Рендерер остаётся серверным и переводит поля\nблока в пропсы островка `components/charts/chart-pie.client.tsx`, где и живёт движение.\n\n## 🔒 ДАННЫЕ НЕОБЯЗАТЕЛЬНЫ\n\nНе прислали своих — рисуется `BROWSER_SHARE` из `components/charts/sample-data.ts`, тот самый массив\nиз исходника владельца. Пустая карточка на витрине читается как поломка вида.",
      "hasCard": true,
      "usedOn": []
    },
    {
      "id": "0051",
      "codes": [
        "chartRadar01"
      ],
      "kind": "chartRadar",
      "type": "charts",
      "shape": "chart",
      "fields": "title: string; description?: string; rows?: ChartRow[]; labels?: { a?: string; b?: string }; footer?: { note?: string; hint?: string }",
      "title": "лепестковая: профиль по нескольким мерам",
      "description": "Фигура по кругу: у каждой меры своя ось, два ряда наложены. Легенда снизу, подвал под ней.\n\n## 🔒 ОТРИЦАТЕЛЬНЫЕ ОТСТУПЫ ПЕРЕНЕСЕНЫ КАК ЕСТЬ\n\n`top: -40`, `bottom: -10` — не опечатка источника. Они прижимают фигуру к легенде и убирают\nпустую полосу, которую диаграмма оставляет под несуществующее заглавие. «Поправленные» на ноль\nотступы дают карточку с дырой сверху.\n\n## 🔒 ПЕРВЫЙ РЯД ПОЛУПРОЗРАЧЕН, ВТОРОЙ НЕТ\n\nДве сплошные заливки друг поверх друга скрывают нижнюю целиком. На лепестковой это не «наложение\nцветов», а потеря ряда.\n\n## Когда НЕ брать\n\n- Мер больше шести — подписи по кругу перестают читаться.\n- Нужны точные величины: по кругу их не считывают, для этого таблица или столбцы.\n- Мера одна — это любой из линейных видов.\n\n## Откуда взялась форма\n\nПеренесена с `https://ui.shadcn.com/charts/bar#charts` 2026-08-30 по прямому указанию владельца:\n«мы создадим в нём по одной диаграмме, и я тебе пришлю их исходный код». Исходник лежит в учёте\nшага 58, папка `new-steps/58/sources/`.\n\n## 🔒 ЦВЕТ РЯДА — РОЛЬ ПАЛИТРЫ, А НЕ ЗНАЧЕНИЕ В КОДЕ\n\nРешение владельца 2026-08-30, дословно: «цвета диаграмм — часть палитры дизайна». Ряды берут\n`var(--chart-1)` … `var(--chart-5)`; умолчание живёт в теме, человек правит их на\n`/{lang}/architect/design?section=colors`, и правка видна без пересборки. Записать цвет прямо в\nблок нельзя: он пережил бы смену палитры и остался единственным пятном старого оформления.\n\n## 🔒 ВИД СОСТОИТ ИЗ ДВУХ ПОЛОВИН\n\nНи один файл под `sections/` не бывает клиентским. Рендерер остаётся серверным и переводит поля\nблока в пропсы островка `components/charts/chart-radar.client.tsx`, где и живёт движение.\n\n## 🔒 ДАННЫЕ НЕОБЯЗАТЕЛЬНЫ\n\nНе прислали своих — рисуется `MONTHS_6` из `components/charts/sample-data.ts`, тот самый массив\nиз исходника владельца. Пустая карточка на витрине читается как поломка вида.",
      "hasCard": true,
      "usedOn": []
    },
    {
      "id": "0052",
      "codes": [
        "chartRadial01"
      ],
      "kind": "chartRadial",
      "type": "charts",
      "shape": "chart",
      "fields": "title: string; description?: string; shares?: ChartShareRow[]; footer?: { note?: string; hint?: string }",
      "title": "радиальные полосы: доля и её величина сразу",
      "description": "Кольцо полос, у каждой своя дорожка. Подписи лежат внутри полос, подвал под диаграммой.\n\n## 🔒 УГЛЫ ПЕРЕНЕСЕНЫ ЧИСЛАМИ ИСТОЧНИКА\n\n`startAngle: -90`, `endAngle: 380` — чуть больше полного оборота. Нахлёст в десять градусов\nделает кольцо замкнутым на вид; ровные -90…270 оставляют видимый стык.\n\n## 🔒 ПОДПИСЬ ПЕРЕЖИВАЕТ ЛЮБУЮ ПАЛИТРУ\n\nБелый текст со смешиванием по яркости: на светлой полосе темнеет, на тёмной остаётся светлым.\nПоэтому пять цветов, выбранных владельцем, не могут сделать подпись нечитаемой.\n\n## Когда НЕ брать\n\n- Важно именно деление целого на части — это `chartPie`.\n- Долей больше пяти: ролей цвета пять, шестая осталась бы без своей.\n- Значения сильно различаются по порядку: короткие полосы становятся невидимы.\n\n## Откуда взялась форма\n\nПеренесена с `https://ui.shadcn.com/charts/bar#charts` 2026-08-30 по прямому указанию владельца:\n«мы создадим в нём по одной диаграмме, и я тебе пришлю их исходный код». Исходник лежит в учёте\nшага 58, папка `new-steps/58/sources/`.\n\n## 🔒 ЦВЕТ РЯДА — РОЛЬ ПАЛИТРЫ, А НЕ ЗНАЧЕНИЕ В КОДЕ\n\nРешение владельца 2026-08-30, дословно: «цвета диаграмм — часть палитры дизайна». Ряды берут\n`var(--chart-1)` … `var(--chart-5)`; умолчание живёт в теме, человек правит их на\n`/{lang}/architect/design?section=colors`, и правка видна без пересборки. Записать цвет прямо в\nблок нельзя: он пережил бы смену палитры и остался единственным пятном старого оформления.\n\n## 🔒 ВИД СОСТОИТ ИЗ ДВУХ ПОЛОВИН\n\nНи один файл под `sections/` не бывает клиентским. Рендерер остаётся серверным и переводит поля\nблока в пропсы островка `components/charts/chart-radial.client.tsx`, где и живёт движение.\n\n## 🔒 ДАННЫЕ НЕОБЯЗАТЕЛЬНЫ\n\nНе прислали своих — рисуется `BROWSER_SHARE` из `components/charts/sample-data.ts`, тот самый массив\nиз исходника владельца. Пустая карточка на витрине читается как поломка вида.",
      "hasCard": true,
      "usedOn": []
    },
    {
      "id": "0053",
      "codes": [
        "chartTooltip01"
      ],
      "kind": "chartTooltip",
      "type": "charts",
      "shape": "chart",
      "fields": "title: string; description?: string; rows?: ChartRow[]; labels?: { a?: string; b?: string }; openAt?: number",
      "title": "подсказка, видимая без наведения",
      "description": "Шесть дней, два ряда с накоплением, и открытая подсказка на второй точке. Указателя у подсказки\nнет, подсветки курсора тоже.\n\n## 🔒 СМЫСЛ ВИДА — ПОДСКАЗКА, А НЕ СТОЛБЦЫ\n\nСтолбцы взяты нарочно простые: смотреть надо не на них. Поэтому подсказка открыта сразу —\n`openAt`. Закрытая подсказка превратила бы вид в обычную маленькую диаграмму, и человек, пришедший\nпосмотреть на подсказку, ушёл бы ни с чем.\n\n## 🔒 СКРУГЛЕНИЯ РАЗНЫЕ У ДВУХ РЯДОВ\n\nНижний ряд скруглён снизу, верхний сверху: вместе они дают один столбик со скруглёнными концами.\nОдинаковые скругления превратили бы стопку в два кирпича.\n\n## Когда НЕ брать\n\n- Нужны данные, а не показ подсказки: это `chartBar` или `chartArea`.\n- Точек много: открытая подсказка на длинном ряде закрывает собой диаграмму.\n\n## Откуда взялась форма\n\nПеренесена с `https://ui.shadcn.com/charts/bar#charts` 2026-08-30 по прямому указанию владельца:\n«мы создадим в нём по одной диаграмме, и я тебе пришлю их исходный код». Исходник лежит в учёте\nшага 58, папка `new-steps/58/sources/`.\n\n## 🔒 ЦВЕТ РЯДА — РОЛЬ ПАЛИТРЫ, А НЕ ЗНАЧЕНИЕ В КОДЕ\n\nРешение владельца 2026-08-30, дословно: «цвета диаграмм — часть палитры дизайна». Ряды берут\n`var(--chart-1)` … `var(--chart-5)`; умолчание живёт в теме, человек правит их на\n`/{lang}/architect/design?section=colors`, и правка видна без пересборки. Записать цвет прямо в\nблок нельзя: он пережил бы смену палитры и остался единственным пятном старого оформления.\n\n## 🔒 ВИД СОСТОИТ ИЗ ДВУХ ПОЛОВИН\n\nНи один файл под `sections/` не бывает клиентским. Рендерер остаётся серверным и переводит поля\nблока в пропсы островка `components/charts/chart-tooltip.client.tsx`, где и живёт движение.\n\n## 🔒 ДАННЫЕ НЕОБЯЗАТЕЛЬНЫ\n\nНе прислали своих — рисуется `SPORT_6` из `components/charts/sample-data.ts`, тот самый массив\nиз исходника владельца. Пустая карточка на витрине читается как поломка вида.",
      "hasCard": true,
      "usedOn": []
    },
    {
      "id": "0054",
      "codes": [
        "orbitLayers01"
      ],
      "kind": "orbitLayers",
      "type": "product-demo",
      "shape": "cards",
      "fields": "badge?: string; title: string; accent?: string; lead?: string; core?: FeatureIcon; cards: [OrbitLayerCard, OrbitLayerCard, OrbitLayerCard, OrbitLayerCard]",
      "title": "орбита: четыре опоры как одна конструкция",
      "description": "Ярлык, заголовок с акцентной второй половиной, подзаголовок. Ниже — орбита: три кольца, медленно\nвращающийся сектор и значок в центре. До `lg` карточки идут сеткой под орбитой, от `lg` стоят по\nчетырём углам поля, а орбита — в его середине.\n\n## Откуда взялась форма\n\n**Перенесена из виджета** `app/[lang]/(publicLayer)/_widgets/static/security-orbit` 2026-08-30 по\nпрямому указанию владельца:\n\n> «Значит мы можем сейчас перенести этот блок в блоки, правильно? Пусть остаётся и в виджетах, и в\n> блоках, а я потом в виджеты придумаю, что поставить — to product-demo»\n\n🔒 **ЭТО КОПИЯ, А НЕ ОБЩАЯ БИБЛИОТЕКА, И РАЗОЙТИСЬ ИМ ПОЗВОЛЕНО.** Импортировать разметку из папки\nвиджета значило бы протянуть зависимость из `sections/` внутрь маршрута: виджет перестал бы быть\nудаляемым, а опыт удаления — единственная его приёмка. Владелец сказал, что виджет он заменит.\n\n## 🔒 ЧТО ЭТОТ ВИД ДОКАЗЫВАЕТ САМИМ СВОИМ СУЩЕСТВОВАНИЕМ\n\nВопрос владельца звучал так: «можем ли мы реализовать эту секцию внутри блока или не можем, потому\nчто она имеет Motion и это конфликтует с нашей стратегией?»\n\n**Можем.** Рендерер под `sections/` остаётся серверным и печатает статического близнеца; движение\nживёт в островке `components/orbit-layers/swap.client.tsx` и приходит поверх уже нарисованного.\n**Наличие анимации никогда не решает «блок или виджет» — решает переиспользование:** вид каталога\nобязан подходить любой странице проекта, виджет принадлежит одному маршруту.\n\n## 🔒 КАРТОЧЕК РОВНО ЧЕТЫРЕ, И ЭТО ГЕОМЕТРИЯ\n\nОт `lg` раскладка ставит их по четырём углам поля, между которыми стоит орбита. Пятой карточке там\nнет места; при трёх один угол пустует и рисунок разваливается. Число закреплено кортежем в типе —\nошибка ловится компилятором, а не глазами на странице.\n\nПорядок углов значим и перенесён как есть: **01 сверху слева, 03 сверху справа, 02 снизу слева,\n04 снизу справа** — читается по диагонали.\n\n## 🔒 ЗНАЧОК НАЗЫВАЕТСЯ СЛОВОМ\n\n`icon` — имя из общего словаря `FeatureIcon`, а не порядковый номер. В источнике значки лежали\nмассивом и раздавались по индексу: вставка карточки посередине молча сдвигала все остальные.\n\n## 🔒 ЗАГОЛОВОК РАЗОРВАН НАДВОЕ\n\n`title` и `accent` — две половины одной фразы; вторая рисуется градиентом. Одной строкой это не\nделается: материал не несёт разметку внутри текста.\n\n## Движение: что происходит на самом деле\n\n- сервер печатает **покой** — всё видно с выключенным JavaScript;\n- анимированная версия грузится отдельным куском и только после того, как указатель вошёл в область\n  или человек нажал;\n- **на телефоне движения нет вовсе** (порог `md`, 767px): там подмена стоит дороже всего и будится\n  только нажатием, то есть в ответ на попытку пролистнуть;\n- «уменьшить движение» уважается: подмена происходит, вращение и пульсация — нет.\n\n## Когда НЕ брать\n\n- Опор не четыре — это `cards`, `featureGrid` или `benefitCards`.\n- Нужен перечень умений продукта, а не конструкция из независимых слоёв, — это `featureGrid`.\n- Секция должна быть уникальной и принадлежать одной странице — это **виджет**, а не вид каталога.",
      "hasCard": true,
      "usedOn": []
    },
    {
      "id": "0056",
      "codes": [
        "chat01"
      ],
      "kind": "chat",
      "type": "workspace",
      "shape": "list",
      "fields": "title?: string; note?: string; size?: 'compact' | 'tall'; messages: ChatBlockMessage[]",
      "title": "`chat` — переписка как секция страницы",
      "description": "Лента сообщений: кто, когда, что написал и что приложил. Рисует её **инструмент проекта**\n`_tools/chat`, а вид — тонкая обёртка над ним: заголовок, рамка, высота и перевод полей блока в\nпропсы островка.\n\n## 🔒 Одна реализация переписки на весь проект — решение владельца\n\nСлова владельца, шаг 80, дословно: **чат — ИНСТРУМЕНТ в `_tools/`, а вид блока — тонкая обёртка над\nним.** Довод его же: «Логи» Telegram-бота живут в слое архитектора, а не на странице из блоков;\nсуществуй чат только блоком — логам пришлось бы рисовать блок ради ленты сообщений.\n\nОтсюда три потребителя одной реализации: **вид блока** (этот файл), **слой архитектора** («Логи») и\nбудущий мессенджер. Своей вёрстки ленты ни у кого из них нет и не заводится.\n\n✗ **Чем это оплачено.** Лента «Логов» (77-5) была сложена руками из элементов списка — со своей\nработой по прокрутке и своим видом сообщения — при том, что библиотека AI Elements уже лежала в\nрепозитории. Второй узор переписки в одном продукте расходится с первым молча.\n\n## 🔒 Вид всегда только лента, без поля ввода\n\nПоле ввода появляется у инструмента, когда ему дан обработчик отправки. Обработчик — функция, а\nфункцию серверный рендерер в клиентский островок не передаёт: под `sections/` не бывает клиентских\nфайлов. **Значит вид блока читается, но не отвечает.**\n\nНужен работающий чат — его ставит потребитель со своим клиентским владельцем состояния, как это\nделают «Логи». Сказано вслух намеренно: молчаливое отсутствие поля ввода читается как дефект вида, а\nне как его граница.\n\n## 🔒 Сообщения — материал страницы, а не запрос к складу\n\nБлок носит с собой то, что показывает. Поэтому секция переносима между поверхностями и рисуется\nодинаково в статье, в каталоге блоков и в машинной версии. Живая лента из склада — дело потребителя,\nкоторый умеет опрашивать и держать курсор; инструмент в сеть не ходит вовсе.\n\n## Поля\n\n| Поле | Что делает |\n|---|---|\n| `title`, `note` | заголовок и строка под ним. Нет — лента стоит сама по себе |\n| `size` | `compact` — 320px, `tall` — 560px, не дано — 420px |\n| `messages` | сообщения: `id`, `from` (`user` \\| `assistant`), `text`, `at`, `who`, `source`, `forwardedFrom`, `attachments` |\n\n**Вложений шесть родов:** `audio`, `image`, `video`, `document` библиотека узнаёт сама\nпо типу содержимого; `place` (широта, долгота, подпись) и `event` (время, заголовок, примечание) —\nнаши, их в библиотеке нет вовсе.\n\n🔒 **Различитель вложения зовётся `type`, а не `kind`.** В `lib/content/blocks/types.ts` слово `kind`\nозначает вид секции — так его читает `check:sections` регуляркой по всему файлу. Вложение,\nу которого различитель назван тем же словом, завело три несуществующих вида каталога, и сторож\nчестно потребовал у них образцы.\n\n## 🔒 Пометка канала лежит пустой намеренно\n\nРешение владельца, 80-2: **пометка источника сообщения понадобится, когда появится второй канал.**\nПоле `source` есть в материале и рисуется, когда заполнено; пока канал один, оно не значит ничего.\nЗаводить его в тот день значило бы трогать договор у всех трёх потребителей разом.\n\n## Когда НЕ брать\n\n- Нужна живая переписка с ответами — это не вид блока, а потребитель со своим состоянием.\n- Нужен журнал службы — «Логи» слоя архитектора берут инструмент напрямую, без вида.\n- Нужны две-три реплики как иллюстрация в тексте — дешевле `quote` или `list`: лента несёт\n  прокрутку, пустое состояние и высоту, которых цитате не нужно.",
      "hasCard": true,
      "usedOn": []
    },
    {
      "id": null,
      "codes": [
        "domainLadder01"
      ],
      "kind": "domainLadder",
      "type": "page-material",
      "shape": "text",
      "fields": "lang: string; words: import('@/components/domain/domain-ladder.i18n').DomainLadderWords",
      "title": null,
      "description": null,
      "hasCard": false,
      "usedOn": []
    },
    {
      "id": null,
      "codes": [
        "servicePort01"
      ],
      "kind": "servicePort",
      "type": "page-material",
      "shape": "text",
      "fields": "serviceId: string; words: import('@/components/services/service-port.i18n').ServicePortWords",
      "title": null,
      "description": null,
      "hasCard": false,
      "usedOn": []
    },
    {
      "id": null,
      "codes": [
        "authGoogleSetup01"
      ],
      "kind": "authGoogleSetup",
      "type": "page-material",
      "shape": "text",
      "fields": "words: import('@/components/auth/google-setup.i18n').GoogleSetupWords",
      "title": null,
      "description": null,
      "hasCard": false,
      "usedOn": []
    },
    {
      "id": null,
      "codes": [
        "authResendSetup01"
      ],
      "kind": "authResendSetup",
      "type": "page-material",
      "shape": "text",
      "fields": "words: import('@/components/auth/resend-setup.i18n').ResendSetupWords",
      "title": null,
      "description": null,
      "hasCard": false,
      "usedOn": []
    }
  ]
}
