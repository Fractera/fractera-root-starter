import { meta } from './meta'
import { en } from './en'
import { ru } from './ru'
import type { FooterPageData } from '@/lib/pages/footer-page'

// 🔒 ДВЕ ЯЧЕЙКИ, А НЕ ДЕСЯТЬ — РЕШЕНИЕ ВЛАДЕЛЬЦА 2026-08-30, дословно: «return
// ru, en another to remove».
//
// Здесь стояли ещё восемь: `es fr it de pt pl tr nl`. Они переводили ПРЕЖНЮЮ
// структуру документа — тринадцать равноправных разделов, — а документ перестроен
// в три столпа. Перевод текста, которого больше нет, хуже отсутствующего: он
// выглядит настоящим, и человек, читающий по-испански, узнаёт об архитектуре не
// то, что читает человек по-английски.
//
// 🛑 ЦЕНА НАЗВАНА ВСЛУХ: пока перевод не вернулся, восемь адресов отдают
// английский текст целиком — по поключевому резолверу, и заголовок тоже. Сборку
// это не роняет (`check:content` в `prebuild` идёт без `--strict`), а ручной
// `npm run check:content` честно предупреждает, пока долг не закрыт.

export const data: FooterPageData = { meta, en, overrides: { ru } }
